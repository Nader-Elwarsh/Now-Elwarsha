# v11-100 — 2026-09-16 — دمج نسخة v11-99 مع تحسينات الفرع الموازي (بدون حذف أي ميزة) + عطل جوهري في saveJSONSafe

النسخة دي هي v11-99 (بكل مميزاتها: زرار حذف أمر شغل مفرد، معاينة خط السير القادم، ربط أزرار الطباعة/المشاركة بـ aria-label، إصلاح كاش `settings-events.js`) كأساس، مدموج معاها كل تحسينات الفرع الموازي "improved" **بدون حذف ولا تعطيل أي ميزة موجودة أو تغيير طريقة استخدامها**:

- 🐛 **عطل جوهري في `saveJSONSafe` (موجود من زمان، من قبل أي مراجعة):** كانت `function saveJSONSafe(k,v){try{put(k,v);return true}catch(e){...}}` — بترجّع `true` **دايمًا** بغض النظر عن نجاح الحفظ الحقيقي، لأن `put()` بتمسك أي خطأ حفظ (امتلاء المساحة، تعطّل localStorage) جوّاها هي نفسها وترجع `false` من غير أي `throw` — يعني الـ `catch` هنا ما كان بينفّذ أبدًا. النتيجة: أكتر من 40 مكان في النظام (حذف كود عطل، حذف صنف، إضافة سريعة، سحب للورشة، تغيير حالة أمر شغل، حركات المحفظة والخزنة...) كانت بتكمّل وكأن الحفظ نجح حتى لو فشل فعليًا بسبب امتلاء المساحة. تم تصحيحها لترجع نتيجة `put()` الحقيقية.
- 🔧 **توسيع تغطية `saveJSONSafe`** في أماكن كانت لسه بتستخدم `put()` خام من غير فحص: حذف كود عطل، حذف/استرجاع صنف من المخزن، الإضافة السريعة، `share-target.js`، الخزنة، والمحفظة — كل دي بقت توقف العملية وتُعلم المستخدم لو الحفظ فشل بدل ما تكمل بصمت.
- 🔄 **تجميع الحذف المزدوج لحركات التحويل (محفظة ↔ خزنة) في عملية واحدة ذرية** (`commitStorage`) بدل عمليتين منفصلتين، عشان ميحصلش نص تحويل يتمسح والنص التاني يفضل لو فشلت العملية الثانية بمفردها.
- ⚡ **تقليل كتابات `localStorage` غير الضرورية:** دالة `settings()` كانت بتكتب نسخة الإعدادات مرة تانية في كل استدعاء حتى لو مجرد قراءة، حتى لو مفيش أي تغيير فعلي. بقت تقارن أولًا وتكتب بس لو فيه فرق حقيقي (زي إضافة قيمة افتراضية جديدة بعد تحديث).
- 🧊 **تعديل الإضافات اليدوية لأكواد الكباسات في مكانها** (مش حذف وإعادة إدخال بس): من صفحة مراجعة التكرارات وصفحة الأكواد نفسها، زرار "✏️ تعديل" بيفتح نموذج الإضافة معبّأ بالبيانات الحالية ويحدّثها في مكانها بنفس المعرّف، بدل ما يضطر المستخدم يحذف ويضيف من الأول.
- 🎨 **تباين ألوان خط السير في الوضع الداكن:** خلايا بيانات أمر الشغل وأزرار الترتيب (⬆️⬇️) في `route.html` كانت بألوان فاتحة ثابتة حتى في الوضع الداكن — بقت متغيرات CSS تتلون تلقائيًا حسب الوضع الحالي (فاتح/داكن).
- ✅ إضافة الاختبارات المقابلة في `core-tests.js` (فشل الحفظ يترجم فعليًا لـ `false`)، `critical-path-smoke.js` (تعديل الإضافات اليدوية)، و`ui-quality-check.js` (تباين ألوان خط السير).
- ✅ تشغيل `npm run check` كاملًا بعد كل التعديلات: نجاح جميع الفحوصات، بلا أي حذف أو تغيير في سلوك أي ميزة موجودة.
- تحديث كاش Service Worker إلى `workshop-v11-100`.

# v11-99 — 2026-09-16 — مراجعة شاملة: دوال ميتة، فجوة تخزين أوفلاين، وصول أفضل

- 🐛 **إصلاح فجوة حقيقية في التخزين الأوفلاين:** `settings-events.js` كان محمّلًا فعليًا من `settings.html` (وهو المسؤول عن التصدير/الاستيراد/فحص السلامة/الحذف وأزرار القوائم في هذه الصفحة) لكنه لم يكن موجودًا في قائمة `CORE_FILES` بملف `service-worker.js`. عمليًا: أي تثبيت PWA بيفتح صفحة الإعدادات أوفلاين بعد انقطاع الشبكة كان ممكن يفشل في تحميل الملف ده وتتعطل أزرار الصفحة. تمت إضافته لقائمة الكاش.
- ✅ **إضافة فحص دائم يمنع تكرار نفس الفجوة:** `project-quality-check.js` بقى يقارن كل ملفات JS/CSS المحمّلة من أي صفحة HTML بقائمة `service-worker.js`، ويفشل الفحص فورًا لو أي ملف محمّل من صفحة وناقص من الكاش الأوفلاين.
- 🧹 **حذف دالة ميتة مكررة `removeTreasuryEntry`** في `treasury.js` (stub قديم `return false` بلا أي استدعاء — الوظيفة الحقيقية اسمها `deleteTreasuryEntry` من زمان).
- 🔌 **ربط دالة `makeActions` غير المستخدمة** في `print-share.js` (كانت نسخة أفضل من أزرار الطباعة/المشاركة بها `aria-label` لقارئ الشاشة) عبر `window.psInlineActions`، فبقت كل استدعاءات `psActions` في النظام (عملاء، أجهزة، قطع، أكواد أعطال، أوامر شغل...) بتاخد نفس النسخة الأوضح لإمكانية الوصول بدل نسخة احتياطية أنقص كانت هي الشغالة فعليًا.
- 🗺️ **تفعيل دالة `renderRouteSummary` الميتة** (معاينة المواعيد القادمة مجمّعة حسب اليوم/المركز/القرية، بتنسيق ألوان قابل للتخصيص) — كانت مكتملة بالكامل ومربوطة بإعداد "🎨 مظهر خط السير القادم" في صفحة الإعدادات، لكن محدش كان بينادي عليها فعليًا فكان تغيير الإعداد بلا أي تأثير ظاهر. بقت تظهر أعلى قائمة أوامر الشغل في `requests.html`، وتم تصحيح نص التلميح في `settings.html` ليعكس مكانها الفعلي.
- 🗑️ **تفعيل زر حذف أمر شغل مفرد** في `request.html`: الدالة `deleteRequestRecord` كانت مكتملة وآمنة (بترفض حذف أمر مغلق/مدفوع، بترجّع القطع للمخزن، بتنضّف التسجيلات المرفقة) لكن من غير أي زر بينادي عليها. أُضيف زر "🗑️ حذف" بجانب زر التعديل (يظهر فقط لو الأمر قابل للتعديل)، وبعد الحذف بيرجّع المستخدم لقائمة الأوامر.
- ✅ تشغيل `npm run check` كاملًا بعد كل التعديلات: نجاح جميع الفحوصات.
- تحديث كاش Service Worker إلى `workshop-v11-99`.

# v11-98 — 2026-09-16 — بحث الكباسات في البحث الشامل + بدائل من شركات تانية + مشاركة الإضافات

- 🔍 **دمج أكواد الكباسات في البحث الشامل (global-search.js).** قسم جديد "🧊 أكواد الكباسات" يظهر في نتائج البحث العائم من أي صفحة بالنظام. قاعدة الكباسات (~11 ألف سجل) بتتحمّل كسول لما المستخدم يفتح صندوق البحث لأول مرة فقط (`window.CompressorRef.ensureAll()`)، مش من تحميل الصفحة، عشان ميتقلش أداء أي صفحة تانية. لو القاعدة اتحمّلت بالفعل (مثلًا المستخدم فاتح صفحة الأكواد قبل كده)، النتائج بتظهر فورًا من غير أي تحميل إضافي.
- 🔌 **`app-compressor-codes.js` بقى محمّل على كل صفحات النظام** (كان قبل كده على `compcodes.html` بس) عشان `window.CompressorRef` يبقى متاح لأي صفحة تحتاجه — تعريف الدوال بس، من غير ما يحمّل أي بيانات فعلية قبل الحاجة الفعلية له.
- 🔄 **بدائل بمواصفات قريبة من شركات تانية.** تحت كل نتيجة بحث في صفحة الأكواد، سطر قابل للفتح "🔄 بدائل بمواصفات قريبة" بيدوّر في نفس القاعدة (مش قاعدة تانية) على كباسات من ماركة مختلفة بنفس الفريون ونفس التطبيق (LBP/HBP) وقدرة قريبة (إزاحة أو BTU ضمن هامش 15%). **مهم:** دي مطابقة مواصفات مسجّلة في القاعدة، مش جدول تعادل معتمد أو موثّق من الشركات المصنّعة نفسها — ظاهر عليها تنويه صريح إن الاستخدام للاسترشاد بس ولازم التأكد من التوافق الفعلي (المقاس، التوصيل الكهربي، نوع وكمية الزيت) قبل الاستبدال. البدائل بتتجاب من القاعدة الأساسية فقط (مش من إضافات يدوية غير موثّقة).
- 📤 **تصدير/استيراد الإضافات اليدوية للكباسات**، بنفس مبدأ مشاركة أكواد الأعطال بالظبط: تصدير ملف JSON منفصل يحتوي بس على الموديلات اللي أضفتها إنت يدويًا (مش القاعدة المرجعية الأساسية اللي أصلاً موجودة عند كل التطبيقات)، واستيراد من فني تاني بيدمج من غير تكرار (بيتجاهل أي موديل موجود بالفعل سواء في القاعدة الأساسية أو في إضافاتك الشخصية).
- ✅ تشغيل `npm run check` كاملًا بعد كل التعديلات، وإضافة تحقّقات جديدة في `project-quality-check.js` و`critical-path-smoke.js` تغطي: وجود `window.CompressorRef`، وجود دالة البدائل والتنويه الخاص بيها، تحميل `app-compressor-codes.js` على الصفحات الأساسية، ووجود أزرار التصدير/الاستيراد الجديدة.
- تحديث كاش Service Worker إلى `workshop-v11-98`.

# v11-97 — 2026-09-16 — تسطيح بنية الملفات بالكامل (بدون مجلدات فرعية)

بناءً على طلب صريح: رفع المشروع يدويًا (مش عن طريق Git) على بعض الأدوات بيصعب فيه اختيار وتحميل مجلدات متداخلة دفعة واحدة، فاتقرر التراجع عن استخدام أي مجلد فرعي والإبقاء على كل ملفات المشروع في مستوى واحد بلا استثناء:

- 🏗️ نقل ملفات الماركات الـ58 من `compressor-brands/` إلى جذر المشروع مباشرة (`brand-000.js` … `brand-057.js`)، وتحديث `compressor-index.js` ليشير إليها بأسمائها المباشرة بدل `compressor-brands/brand-XXX.js`.
- 🏗️ نقل كل ملفات الاختبار من `tests/` إلى جذر المشروع مباشرة، وتحديث `package.json` (`test`/`check`/`browser-test`) والمسارات النسبية داخل كل سكريبت اختبار (`${__dirname}/..` أصبحت `${__dirname}`) لتعمل من نفس المستوى.
- 🏗️ تحديث `browser-smoke.sh` و`browser-interactive.html` لنفس التسطيح (إزالة `..` من المسارات).
- 🏗️ تحديث `PROJECT_STRUCTURE.md` ليعكس عدم وجود أي مجلدات فرعية.
- 🧹 حذف `audit.py` (سكريبت مراجعة شخصي بمسار مؤقت محلي `/tmp/...`، غير مستخدم من أي جزء في التطبيق أو `package.json`).
- لا تغيير في التحميل الكسول لقاعدة الأكواد ولا في أي منطق أو شكل بيانات — كل ملف ماركة لسه بيتحمّل مستقل عند الحاجة بالظبط زي قبل، والتغيير الوحيد هو مكان الملف على القرص.
- ✅ تشغيل `npm run check` كاملًا بعد التسطيح: نجاح جميع الفحوصات.
- تحديث كاش Service Worker إلى `workshop-v11-97`.

# v11-96 — 2026-09-15 — توثيق دمج أكواد الشركات الخمس الرسمية (BITZER, Copeland, Danfoss, Tecumseh, Panasonic)

هذا الإدخال يوثّق دمجًا كان قد طُبّق فعليًا على قاعدة البيانات (`compressor-index.js` وملفات الماركات) قبل هذه النسخة من `CHANGELOG.md` — التفاصيل الكاملة موجودة في `OFFICIAL-FIVE-IMPORT.md` و`official-compressor-import-summary.json` المرفقين مع الحزمة، ولم تكونا موثّقتين هنا سابقًا:

- أُضيفت/دُمجت سجلات رسمية لخمس شركات: BITZER (178 سجل، ملف مستقل جديد)، Copeland (127 سجل مدمجة مع EMERSON-COPELAND)، Danfoss (15 سجل مدمجة مع دانفوس-سيكوب)، Tecumseh (96 سجل مدمجة مع تيكومسيه 1)، Panasonic (792 سجل مدمجة مع باناسونيك).
- أُزيلت 2355 نسخة مكررة على مستوى الشركة والكود بعد التطبيع، ودُمجت 580 قيمة ناقصة من السجلات المكررة من غير الكتابة فوق قيمة موجودة.
- إجمالي القاعدة بعد الدمج: 11165 سجلًا فريدًا عبر 58 شركة/تصنيف (كان 56 قبل إضافة BITZER كملف مستقل).
- لم تُدخل عائلات أو أكواد بلا كود كامل مؤكد في المصدر، ولم تُستنتج قيم الجهد أو المبرد أو التطبيق من اسم الموديل.
- اجتازت النسخة وقتها فحص JavaScript، واختبارات البيانات والترحيل والمعاملات والمسارات الحرجة وجودة المشروع والواجهة، واختبار Chromium الحقيقي.

# v11-95 — 2026-09-15 — تشفير النسخ واختبارات التفاعل

- إضافة تشفير اختياري للنسخ الاحتياطية باستخدام AES-GCM مع اشتقاق مفتاح PBKDF2-SHA-256.
- دعم استيراد النسخ المشفرة مع طلب كلمة المرور والتحقق من صحة التشفير.
- الإبقاء على دعم النسخ العادية غير المشفرة عند ترك كلمة المرور فارغة.
- منع الضغط المزدوج أثناء تصدير أو استيراد النسخ الاحتياطية.
- منع تكرار عملية الحذف التشغيلي أثناء تنفيذها.
- إضافة harness تفاعلي لاختبار Chromium يغير حقل البحث ويضغط زر مراجعة التكرارات ويتحقق من النتيجة.
- تحديث فحص المتصفح ليشمل التحميل والتفاعل، وليس فتح الصفحات فقط.
- تحديث نص الإعدادات لشرح التشفير الاختياري.
- لا يوجد Backend جديد؛ الانتقال إلى Backend ما زال مؤجلًا حتى الحاجة لتعدد الأجهزة أو المستخدمين.
- تحديث كاش Service Worker إلى `workshop-v11-95`.

# v11-94 — 2026-09-15 — تقسيم قاعدة الأكواد واختبارات المتصفح

- تقسيم قاعدة الأكواد إلى `compressor-index.js` و57 ملف ماركة داخل `compressor-brands/`.
- إزالة ملف القاعدة المجمعة القديم لتجنب الاحتفاظ بنسختين من البيانات.
- تحميل الفهرس الصغير أولًا، ثم تحميل ماركة واحدة عند استخدام الفلتر، أو كل الماركات عند البحث الحر.
- تحديث Service Worker ليضم الفهرس وملفات الماركات المقسمة.
- تحويل عناصر التحكم الثابتة في صفحة الإعدادات إلى `data-action` وevent delegation.
- إضافة `settings-events.js` للتصدير والاستيراد وفحص السلامة والحذف وأزرار القوائم الأساسية.
- إضافة اختبار متصفح حقيقي باستخدام Chromium يفتح الصفحة الرئيسية وصفحة الأكواد والإعدادات.
- إضافة أمر `npm run browser-test` منفصل عن فحوص Node.
- لا يوجد Backend جديد؛ النظام ما زال مناسبًا لجهاز واحد، ويظل الانتقال إلى Backend مؤجلًا حتى تظهر حاجة تعدد الأجهزة أو المستخدمين.
- تحديث كاش Service Worker إلى `workshop-v11-94`.

# v11-93 — 2026-09-15 — الدمج الآمن للتكرارات

- إضافة تنزيل خطة JSON لمراجعة تكرارات القاعدة المرجعية قبل أي قرار.
- إضافة دمج اختياري للإضافات اليدوية المتوافقة فقط.
- رفض الدمج الآلي لأي مجموعة تحتوي اختلافًا في الحقول.
- إنشاء نسخة أمان كاملة من الإضافات اليدوية قبل الدمج.
- طلب تأكيد صريح قبل الكتابة، مع استرجاع تلقائي للنسخة الأصلية إذا فشل الحفظ.
- إبقاء سجلات القاعدة المرجعية الأصلية خارج الدمج التلقائي لتجنب فقد مواصفات.
- إضافة اختبارات دخانية لحماية النسخ والتأكيد وعدم الدمج المتعارض.
- تحديث كاش Service Worker إلى `workshop-v11-93`.

# v11-92 — 2026-09-15 — مراجعة تكرارات أكواد الكباسات بأمان

- إضافة زر `مراجعة التكرارات` داخل صفحة أكواد الكباسات.
- تجميع السجلات حسب مفتاح الماركة والموديل المطبّع.
- عرض عدد مجموعات التكرار وعدد السجلات داخل كل مجموعة.
- عرض مصدر السجل والحقول المختلفة لمساعدة المراجعة اليدوية.
- عدم حذف أو دمج أي سجل تلقائيًا؛ الأداة تشخيصية وغير تدميرية.
- إضافة تنسيق متجاوب للوحة المراجعة على الهاتف.
- إضافة اختبار دخاني يتأكد من وجود أداة المراجعة وعدم وجود حذف تلقائي.
- تحديث كاش Service Worker إلى `workshop-v11-92`.

# v11-91 — 2026-09-15 — تحسين واجهة المستخدم وتجربة الاستخدام

- إضافة حالات `aria-live` و`aria-busy` لنتائج وتحميل صفحة أكواد الكباسات.
- إضافة تسميات وصول واضحة للبحث وفلتر الماركة وحقول الإعدادات.
- تحسين التركيز المرئي للوحة المفاتيح وأهداف اللمس للأزرار والحقول.
- تحسين تخطيط أزرار كروت الأكواد والمفضلة على الشاشات الصغيرة.
- إضافة دعم `prefers-reduced-motion` لتقليل الحركة للمستخدمين الذين يفضلون ذلك.
- إضافة حالات تحميل ورسالة خطأ مفهومة عند فشل تحميل قاعدة الأكواد.
- إضافة مناطق حالة قابلة للإعلان في النسخ الاحتياطي وفحص سلامة البيانات.
- إضافة فحص واجهة آلي ضمن `npm run check`.
- الحفاظ على منطق البيانات وطريقة الاستخدام الأساسية.
- تحديث كاش Service Worker إلى `workshop-v11-91`.

# v11-90 — 2026-09-15 — تحسين الأداء والتنظيم الهيكلي

- تخزين النسخة المطبّعة من موديل الكباس مرة واحدة داخل سجل البحث بدل إعادة تطبيعها أثناء كل مقارنة وفرز.
- الإبقاء على فهرس نتائج البحث المحدود مع إبطال الكاش عند تغيّر البيانات.
- إضافة `PROJECT_STRUCTURE.md` لتوثيق طبقات المشروع ومسؤوليات الملفات وقواعد التعديل.
- إضافة `tests/project-quality-check.js` لفحص التحميل الكسول، الروابط الهيكلية، وجود الفهرسة، وحجم قاعدة الأكواد.
- ضم فحص الجودة إلى `npm run check`.
- تحديث فحص الصياغة إلى 45 ملف JavaScript.
- لا تغيير في طريقة الاستخدام أو شكل النظام.
- تحديث كاش Service Worker إلى `workshop-v11-90`.

# v11-89 — 2026-09-15 — event delegation لكروت الأكواد

- إزالة `onclick` من أزرار كروت نتائج أكواد الكباسات.
- إزالة `onclick` من أزرار المفضلة في صفحة الأكواد.
- إضافة event delegation موحّد على حاويتي النتائج والمفضلة.
- استخدام `data-comp-action` و`data-comp-id` بدل تمرير JSON داخل HTML.
- تقليل مخاطر أخطاء escaping وتحسين قابلية صيانة كروت النتائج.
- الحفاظ على نفس وظائف الحذف والمفضلة والنسخ وشكل العرض.
- إضافة فحص دخاني يمنع عودة inline handlers داخل الكروت والمفضلة.
- تحديث كاش Service Worker إلى `workshop-v11-89`.

# v11-88 — 2026-09-15 — تحميل أكواد الكباسات عند الحاجة وتقليل inline events

- إزالة التحميل المباشر لقاعدة `compressor-db.js` من HTML.
- تحميل قاعدة الأكواد عند فتح صفحة الأكواد فقط وبعد تهيئة الموديول.
- إضافة رسالة فشل واضحة إذا تعذر تحميل قاعدة الأكواد.
- تحويل البحث، فلتر الماركة، فتح/إلغاء نموذج الإضافة، والحفظ إلى `addEventListener`.
- تقليل التفاعل المعتمد على `onclick` في عناصر التحكم الأساسية لصفحة الأكواد دون تغيير طريقة الاستخدام.
- إضافة فحص دخاني يؤكد التحميل الكسول وعدم عودة أحداث البحث inline.
- تحديث كاش Service Worker إلى `workshop-v11-88`.

# v11-87 — 2026-09-15 — منع تكرار أكواد الكباسات وتحسين البحث

- إضافة مفتاح مقارنة موحّد للماركة والموديل يتجاهل المسافات والشرطات والشرطات المائلة واختلاف حالة الحروف.
- رفض إضافة موديل يدوي إذا كان موجودًا في القاعدة الأساسية أو الإضافات اليدوية.
- منع ظهور الموديل نفسه أكثر من مرة في نتائج البحث حتى لو وُجد تكرار قديم داخل القاعدة.
- تنظيف التكرارات القديمة في المفضلة والإضافات عند قراءتها.
- منع تكرار المفضلة بالمقارنة الموحّدة بدل المقارنة النصية الحرفية.
- إضافة ذاكرة مؤقتة لنتائج البحث مع مسحها عند تعديل الأكواد.
- الحفاظ على نفس طريقة العرض والنماذج الحالية.
- تحديث كاش Service Worker إلى `workshop-v11-87`.

# v11-86 — 2026-09-15 — rollback كامل للاسترجاع والحذف

- إضافة حفظ حالة localStorage كاملة قبل الاسترجاع والحذف الشامل.
- عند فشل الاسترجاع بعد تغيير الصور أو البيانات، تتم إعادة البيانات والإعدادات والمخطط وتاريخ آخر نسخة للحالة السابقة.
- عند فشل الحذف الشامل، تتم إعادة البيانات والصور بدل ترك النظام في حالة حذف جزئي.
- ترتيب الحذف أصبح ينظف الصور أولًا، ولا يحذف بيانات localStorage إلا بعد نجاح تنظيف الصور.
- استرجاع إعداد الإشعارات الفارغ أصبح يحذف القيمة القديمة بدل تركها.
- إضافة اختبارات rollback لحالة localStorage واختبار دخاني لمسارات الاسترجاع.
- تحديث كاش Service Worker إلى `workshop-v11-86`.

# v11-85 — 2026-09-15 — تقوية النسخ الاحتياطي والاسترجاع

- جعل ملف النسخة الاحتياطية يحتوي على كل أقسام البيانات المعروفة بشكل صريح.
- رفض النسخ الناقصة بدل استبدال جزء من البيانات الحالية وترك أقسام قديمة.
- التحقق من أن كل قسم قائمة أو إعدادات من النوع الصحيح، وأن كل سجل كائن صالح.
- رفض قيم الصور غير النصية، إعداد إشعارات غير صحيح، ومخطط بيانات مستقبلي غير مدعوم.
- تحسين ملخص الاسترجاع ليعرض إصدار المخطط وعدد سجلات الخزنة والصور.
- إضافة اختبارات للنسخة الكاملة، القسم المفقود، السجل المشوه، والمخطط المستقبلي.
- لا يتم تعديل البيانات الحالية قبل التحقق والنسخة الآمنة وتأكيد المستخدم.
- تحديث كاش Service Worker إلى `workshop-v11-85`.

# v11-84 — 2026-09-15 — تقوية مسارات الحفظ والاختبار الدخاني

- جعل إضافة القطعة الخارجية لأمر الشغل تحفظ بنتيجة صريحة مع rollback عند الفشل.
- جعل تعديل أسعار المخزن بالجملة يتحقق من نجاح الحفظ ولا يعرض رسالة نجاح عند فشل التخزين.
- منع الحفظ الصامت في تحديثات حالة وزيارة وتصنيف أوامر الشغل ومسارات المتابعة.
- إضافة اختبار دخاني يتأكد من بقاء عمليات المخزن والطلبات والحفظ الجماعي مرتبطة بمسارات rollback أو فحص نتيجة الحفظ.
- تحديث فحص المشروع إلى 44 ملف JavaScript.
- لا تغيير في طريقة الاستخدام أو شكل الشاشات.
- تحديث كاش Service Worker إلى `workshop-v11-84`.

# v11-83 — 2026-09-15 — معاملات المخزن الذرية

- جعل إضافة صنف جديد وحركة التوريد المرتبطة به عملية واحدة مع rollback.
- جعل توريد كمية لصنف قائم وحركة التوريد عملية واحدة مع rollback.
- عند فشل حفظ أي مفتاح، يتم إرجاع المخزون وحركاته للحالة السابقة بدل ترك بيانات جزئية.
- إضافة اختبار يحاكي فشل التخزين ويتأكد من عودة المفتاحين للحالة الأصلية.
- تحديث فحص المشروع إلى 43 ملف JavaScript.
- لا تغيير في خطوات الاستخدام أو واجهة المخزن.
- تحديث كاش Service Worker إلى `workshop-v11-83`.

# v11-82 — 2026-09-15 — تقوية فحص الاتساق المالي والمخزني

- توسيع فحص سلامة البيانات ليشمل إجمالي القطع، تكلفة القطع، إجمالي الأمر، والعربون.
- فحص مبالغ وأنواع حركات الحسابات والخزنة.
- فحص الروابط المكررة أو المفقودة بين حركات التحصيل وأوامر الشغل.
- فحص طرفي التحويل بين الحساب والخزنة ومطابقة المبلغ بين الطرفين.
- فحص ارتباط حركات المخزن بأوامر شغل موجودة.
- إضافة اختبار بيانات تالفة يتأكد من اكتشاف كمية سالبة وإجمالي خاطئ وعربون غير صالح.
- الفحص تشخيصي فقط ولا يعدل البيانات.
- تحديث كاش Service Worker إلى `workshop-v11-82`.

# v11-81 — 2026-09-15 — جاهزية الترحيلات وفحص سلامة البيانات

- جعل تشغيل الترحيلات مركزيًا باستخدام Promise واحد مشترك يمنع التشغيل المتوازي.
- بدء الترحيل مبكرًا عند تحميل طبقة الترحيلات، مع بقاء الصفحات منتظرة لنفس العملية قبل العرض.
- إضافة فحص سلامة اختياري في الإعدادات لاكتشاف الروابط المفقودة، المعرفات المكررة، الكميات غير الصالحة، وحركات المخزن غير المرتبطة.
- الفحص تشخيصي فقط ولا يعدّل أي بيانات.
- إضافة اختبار single-flight للترحيلات.
- تحديث كاش Service Worker إلى `workshop-v11-81`.

# v11-80 — 2026-09-15 — تحسين الأداء وجودة التحقق

- تحسين بحث قاعدة أكواد الكباسات باستخدام فهرس نصي مؤقت بدل إعادة بناء نص البحث لكل سجل مع كل بحث.
- إعادة بناء الفهرس تلقائيًا فقط عند إضافة أو حذف موديل يدوي.
- إضافة `package.json` وأوامر `npm run test` و`npm run check`.
- إضافة فحص صياغة موحد لجميع ملفات JavaScript واختبار بنية النسخ والتاريخ.
- لا تغيير في واجهة الاستخدام أو طريقة التشغيل.
- تحديث كاش Service Worker إلى `workshop-v11-80`.

# v11-79 — 2026-09-15 — توحيد التاريخ المحلي وإضافة الاختبارات

- توحيد حساب اليوم والشهر في لوحة التحكم والتقارير والإشعارات ومسارات أوامر الشغل باستخدام التوقيت المحلي.
- منع انزياح أوامر اليوم وتقارير الشهر عند اختلاف المنطقة الزمنية عن UTC.
- إضافة اختبارات آلية للتاريخ المحلي وبنية النسخ الاحتياطية، مع استمرار اختبارات الحفظ الذري.
- لا تغيير في واجهة الاستخدام أو خطوات تشغيل النظام.
- تحديث كاش Service Worker إلى `workshop-v11-79`.

# v11-78 — 2026-09-15 — تقوية النسخ الاحتياطي والاسترجاع

- إضافة نسخة أمان تلقائية قابلة للتنزيل قبل استرجاع أي نسخة.
- إضافة تحقق من بنية النسخة وملخص بعدد العملاء والأجهزة والأوامر والقطع والصور قبل الاستبدال.
- تقوية استرجاع الصور والتسجيلات مع محاولة إعادة الحالة السابقة عند الفشل.
- إبقاء أزرار التصدير والاستيراد وطريقة الاستخدام الأساسية كما هي.
- تحديث كاش Service Worker إلى `workshop-v11-78`.

# v11-77 — 2026-09-15 — حماية الحفظ والمعاملات

- إضافة `commitStorage` لحفظ عدة مفاتيح مع rollback تلقائي عند فشل التخزين.
- تطبيق الحفظ الذري على أوامر الشغل والمخزون وحركات المخزن وإضافة القطع والحذف والاستيراد.
- جعل فشل `put()` مرئيًا للعمليات متعددة الخطوات بدل الاستمرار بحالة جزئية.
- تحديث كاش Service Worker إلى `workshop-v11-77`.

# v11-76 — 2026-09-15 — إضافة أكواد كباسات Samsung من الكتالوج المرفق

- إضافة 117 موديل Samsung جديد إلى `compressor-db.js` بعد مقارنة 217 موديلًا مستخرجًا من الكتالوج مع القاعدة الحالية.
- استبعاد 100 موديل موجود مسبقًا لمنع التكرار، مع اعتبار المقارنة غير حساسة لحالة الأحرف والمسافات والشرطات.
- حفظ المواصفات بنفس بنية العرض الحالية (`refrigerant`, `kcal`, `watt`, `btu`, `application`, `extra`, `notes`).
- تحديث كاش Service Worker إلى `workshop-v11-76` وتحديث نسخة manifest حتى تظهر الأكواد الجديدة في التطبيق المثبت.

# v11-75 — 2026-09-14 — دمج قسم "أكواد الكباسات" الجديد + إصلاح عيبين فيه

⚠️ الميزة الجديدة نفسها (`compcodes.html`, `app-compressor-codes.js`,
`compressor-db.js`) جاية من نسخة رفعها المستخدم، ودُمجت هنا فوق v11-74
(كل إصلاحات المظهر/التليفون/المخزون/المحفظة/الأمان من قبل كده لسه موجودة).
مرجع بحث عن قدرة أي كباس بموديله (حصان، BTU، فريون، أمبير، مكثفات، زيت...)
عبر أكتر من 13,600 موديل و56 ماركة، مع إمكانية حفظ مفضلة وإضافة موديلات
ناقصة يدويًا. بمناسبة طلب المستخدم مراجعة قسم الأكواد تحديدًا، تم فحصه
ولوحظ فيه عيبان حقيقيان اتصلحوا في نفس الدمج:

- 🐛 **زرار "⭐ حفظ في المفضلة" كان بيفشل صامتًا مع كل كباس بلا استثناء**:
  الكود كان بيحط بيانات الكباس كاملة كـ JSON جوه `onclick` بعد تنظيفها
  بدالة `esc()` العادية بس. بما إن أي JSON فيه علامات اقتباس مزدوجة كتير
  أصلاً (بحكم بنيته)، والمتصفح بيفك ترميز الـ HTML entities دي *قبل* ما
  ينفّذ الزرار كجافاسكريبت، السترنج كان بيتقفل بدري لكل مرة يتدوس فيها
  الزرار — نفس فئة العيب المكتشف في v11-74 بس هنا بيضرب 100% من الحالات
  مش حالة نادرة. اتصلح بنفس دالة `escAttr()` المستخدمة في باقي النظام.
  نفس الإصلاح اتطبق على أزرار "حذف الإضافة" و"نسخ البيانات" وعلى تشيبس
  "المفضلة" (اللي كانت بتحط اسم الموديل جوه الزرار بطريقة تانية مش آمنة).
- 🐛 **ترتيب قايمة الماركات في فلتر البحث كان مش أبجدي فعليًا**: الكود
  كان بيستخدم `a.localeCompare("ar")` (يقارن كل اسم ماركة بالنص الحرفي
  "ar" نفسه) بدل `a.localeCompare(b, "ar")` (يقارن الماركتين مع بعض
  بترتيب أبجدي عربي) — يعني القايمة (56 ماركة) كانت بترجع بترتيب عشوائي
  تقريبًا بدل الترتيب الأبجدي. اتصلح.
- 🔧 كاش الـ Service Worker اتحدث ليشمل ملفات الميزة الجديدة (`workshop-v11-75`).

# v11-74 — 2026-09-14 — مسح شامل للنظام (بطلب المستخدم) + إصلاح عيب هيكلي في أزرار الإعدادات

⚠️ اتنفذ بمساعدة Claude بناءً على طلب المستخدم بعمل "مسح كلي للنظام" للبحث
عن أي عيوب إنشائية أو تكرار أو دوال بايظة. تم فحص كل ملفات JS (37 ملف) و
HTML (22 صفحة) بشكل منهجي: أخطاء لغوية، تكرار أسماء دوال، مفاتيح تخزين
غير متطابقة، `id` مكرر، قائمة كاش الـ Service Worker، أكواد تصحيح منسية،
كلاسات CSS بلا تنسيق، وأمان الإدخال.

- 🐛 **عيب هيكلي حقيقي في كل أزرار "تعديل/حذف" بصفحة الإعدادات** (مراكز،
  قرى، تصنيفات، ماركات، أنواع، تصنيفات القطع، تبويبات الأمر، أسماء
  المحافظ...): كانت بتحط اسم العنصر جوه `onclick="fn('...')"` بعد تنظيفه
  بدالة `esc()` (اللي مخصصة للعرض داخل HTML بس). المشكلة: لو الاسم فيه
  علامة اقتباس إنجليزي واحدة (') — زي اسم مركز أو ماركة أو تصنيف مكتوب
  بيها — المتصفح كان بيفك ترميزها *قبل* ما ينفّذ الزرار كجافاسكريبت، فالسترنج
  كان بيتقفل بدري والزرار بيبوظ من غير أي رسالة خطأ واضحة للمستخدم (تعديل/حذف
  العنصر ده بس مايشتغلش). ده مش هجوم أمني وارد فعليًا هنا (التطبيق محلي
  لمستخدم واحد)، لكنه عيب وظيفي حقيقي ممكن يحصل بالصدفة البحتة.
  ✅ اتضاف Helper جديد `escAttr()` مخصص بالظبط لتنظيف نص هيوّدع جوه
  `('...')` داخل `onclick`، واستُخدم بدل `esc()` في الـ 32 مكان المتأثرين
  (كلهم في صفحة الإعدادات + المحافظ + تجاهل مكالمة معلّقة). العرض العادي
  للنص في الصفحة لسه بيستخدم `esc()` زي ما هو، من غير أي تغيير.
- ✅ لا توجد تكرارات حقيقية لأسماء الدوال بين الملفات (اللي بدت متكررة
  كانت كلها محبوسة جوه `(function(){...})()` منفصلة).
- ✅ كل مفاتيح `K.*` المستخدمة في كل الملفات متطابقة تمامًا مع تعريفها.
- ✅ قائمة تخزين الـ Service Worker مطابقة 100% للملفات الموجودة فعليًا.
- ✅ لا توجد أكواد تصحيح منسية (`console.log`/`debugger`/علامات تعارض Git).
- ✅ مفيش أي مكان بيدخل نص من إدخال المستخدم جوه `innerHTML` من غير `esc()`.
- 🔧 ترقية كاش الـ Service Worker (`workshop-v11-74`).

# v11-73 — 2026-09-14 — إصلاح فعلي لقائمة المركز/القرية غير المقروءة في الوضع الليلي

⚠️ اتنفذ بمساعدة Claude بناءً على صورة من المستخدم بعد تفعيل الوضع الليلي
الحقيقي (v11-70) بيّنت إن قائمة اقتراحات "المركز" جوه نموذج "إضافة عميل
سريع" لسه غير مقروءة (نص غامق على خلفية غامقة) — يعني مكنتش مجرد قضية
"كروم بيعمل Force Dark لصفحة فاتحة"، كان فيه خلل حقيقي في الـ CSS اتكشف
لما الوضع الداكن بقى بيشتغل صح تلقائيًا.

- 🐛 **السبب الحقيقي**: صندوق "إضافة سريع" (`.quick-add`) خلفيته ثابتة
  فاتحة في الوضعين، فكان مجبور لون نص غامق (`#1a1a1a`) على *كل* العناصر
  اللي جواه في الوضع الليلي عشان يفضل مقروء فوق خلفيته الفاتحة — بما فيها
  قائمة نتائج البحث (المركز/القرية) اللي هي في الحقيقة صندوق مستقل بخلفية
  داكنة حقيقية في الوضع الليلي. النتيجة: نص غامق (موروث من الأب) فوق خلفية
  غامقة (خاصة بالقائمة نفسها) = غير مقروء.
- ✅ **الحل**: قائمة النتائج دي بقت ترجع تستخدم لون النص الطبيعي للوضع
  الداكن بدل ما ترث اللون الغامق المجبور من صندوق "إضافة سريع"، وبالتبعية
  بقت مقروءة تمام (فاتح على غامق) في كل حقول البحث جوه "إضافة عميل سريع"
  (المركز، القرية، وأي قائمة تانية مشابهة).
- 🔧 تحسين بسيط: الخط الفاصل بين عناصر أي قائمة بحث (زي قطع الغيار) بقى
  يتلوّن حسب الثيم الحالي بدل لون رمادي فاتح ثابت كان بيبان زيادة في
  الوضع الداكن.
- 🔧 ترقية كاش الـ Service Worker (`workshop-v11-73`).

# v11-72 — 2026-09-14 — دمج إصلاحات المخزون والمحفظة (نسخة مصحَّحة من المستخدم)

⚠️ الملفات التلاتة دول جاية من نسخة "مصحَّحة" رفعها المستخدم بنفسه (مش من
تحليل Claude)، وتم دمجها هنا فوق إصلاحات المظهر والتليفون في v11-70/v11-71
من غير أي تعديل عليها:

- **app-delete-tools.js**: عند حذف أمر شغل (فردي أو جماعي)، الأوامر
  الملغية بقت مستثناة من "إرجاع القطع للمخزن" (لأنها أصلاً رجعت وقت
  الإلغاء، فإرجاعها تاني كان بيكرر الكمية)، وحذف الأمر بقى كمان بيمسح أي
  حركة عربون/تحصيل نهائي مرتبطة بيه في المحفظة (مش الخزنة بس زي قبل كده).
- **app-requests.js**: إصلاح ازدواج تعديل المخزون عند تغيير حالة الأمر
  إلى/من "ملغي" — كان بيتعدّل مرتين لنفس التغيير، دلوقتي مرة واحدة بس.
- **wallets.js**: تحويل "محفظة ↔ خزنة" بقى ذرّي (atomic) — لو فشل تسجيل
  أي طرف من الطرفين، الاتنين مايتسجلوش والمستخدم يشوف رسالة خطأ بدل تحويل
  نصّه مسجل.
- 🔧 ترقية كاش الـ Service Worker (`workshop-v11-72`).

# v11-71 — 2026-09-14 — إصلاح رابط الاتصال (تليفون) اللي مش بيفتح بعد تثبيت التطبيق من كروم

⚠️ اتنفذ بمساعدة Claude بناءً على ملاحظة المستخدم إن الدوس على رقم التليفون
(📲) في أمر الشغل بيفتح صفحة خطأ "This site can't be reached /
ERR_UNKNOWN_URL_SCHEME" — والمشكلة دي ظهرت بس لما التطبيق يكون متثبّت
("إضافة إلى الشاشة الرئيسية") من متصفح **كروم** تحديدًا؛ نفس التطبيق كان
شغال تمام لما يتثبّت من متصفح سامسونج.

- 🐛 **السبب**: نافذة التطبيق المثبّتة من كروم (PWA) بتفتح كـ"تطبيق مستقل"
  من غير شريط عنوان، وفي الوضع ده كروم مش بيقدر يسلّم رابط بروتوكول غير
  http/https (زي `tel:`) لتطبيق الاتصال بتاع الهاتف لو الرابط فتح "في نفس
  نافذة التطبيق" — فبيظهر بدل الاتصال صفحة خطأ. رابط الواتساب كان شغال
  عادي لأنه أصلاً بيفتح "في نافذة جديدة" (`target="_blank"`)، وده اللي كان
  فارق الفعلي بينه وبين رابط التليفون.
- ✅ **الحل**: رابط رقم التليفون (📲) بقى يفتح هو كمان بنفس طريقة الواتساب
  ("نافذة جديدة")، وده بيخلي كروم يسلّم الرابط بشكل صحيح لتطبيق الاتصال
  في كل الصفحات (العميل، الجهاز، أمر الشغل، خط السير، متابعة العملاء
  الساكتين... إلخ) لأنها كلها بتستخدم نفس الدالة المشتركة لرابط التليفون.
- 🔧 ترقية كاش الـ Service Worker (`workshop-v11-71`).

⚠️ **مهم بخصوص إصلاح قائمة "القرية" في v11-70**: لو لسه شايف نفس مشكلة
القائمة غير المقروءة بعد رفع النسخة دي، الاحتمال الأكبر إن التحديث لسه ما
وصلش فعليًا لنسخة التطبيق المثبّتة عندك — التطبيقات المثبّتة من كروم بتفضل
شغالة بنسخة قديمة متخزنة لحد ما تتاكد من: (١) إنك رفعت كل ملفات المجلد ده
على نفس مكان الاستضافة اللي التطبيق شغال منه، و(٢) قفلت التطبيق المثبّت
تمامًا (مش بس رجعت للخلف) وفتحته تاني، أو في حالة استمرار المشكلة جرّب تحذف
أيقونة التطبيق من الشاشة الرئيسية وتثبتها تاني من كروم بعد رفع التحديث.

# v11-70 — 2026-09-14 — المظهر (فاتح/داكن) بيتبع مظهر الهاتف تلقائيًا

⚠️ اتنفذ بمساعدة Claude بناءً على ملاحظة المستخدم إن قائمة "القرية" (وغيرها)
في نموذج "إضافة عميل سريع" جوه أمر الشغل السريع كانت طالعة بشكل غير مقروء
تحت الوضع الليلي للهاتف.

- 🐛 **السبب الحقيقي**: النظام كان دايمًا افتراضيًا "فاتح" (`data-theme` من
  غير أي قيمة) لحد ما المستخدم يدوس بنفسه على زرار 🌙 ويحفظ اختياره — يعني
  ما كانش بيتابع وضع الهاتف (فاتح/داكن) أبدًا. المتصفح كان شايف إن الصفحة
  "فاتحة" رسميًا بينما الهاتف في الوضع الليلي، فبيحاول هو نفسه يـ"يعكس"
  ألوان بعض العناصر (زي قائمة نتائج البحث المنسدلة) تلقائيًا بطريقة بايظة
  وغير مقروءة — وده اللي ظهر في الصورة.
- 🆕 **المظهر بقى تلقائي فعليًا**: أول ما تفتح أي صفحة، النظام بيشوف مظهر
  الهاتف الحالي (`prefers-color-scheme`) ويطبّقه على طول (فاتح أو داكن)، وبيفضل
  يتابعه لحظيًا لو الهاتف غيّر وضعه وانت فاتح الصفحة (مثلاً الوضع الليلي
  بدأ تلقائيًا وقت الغروب).
- 🆕 **التحكم اليدوي فاضل موجود**: زرار 🌙/☀️ في الهيدر لسه بيغيّر المظهر
  فورًا وقت ما تحب، والاختيار ده بيتحفظ ويكسر التتابع التلقائي في هذا
  المتصفح لحد ما تمسح بيانات الموقع (يعني تقدر تفرض فاتح أو داكن براحتك
  في أي وقت، مش لازم تسيبها تلقائي).
- 🔧 أضيف `<meta name="color-scheme" content="light dark">` في كل الصفحات
  عشان يأكّد للمتصفح إن الصفحة بتدعم الوضعين رسميًا فيبطل يحاول "يصلّح"
  الألوان بنفسه.
- 🔧 ترقية كاش الـ Service Worker (`workshop-v11-70`) عشان `theme.js`
  والصفحات المحدّثة توصل فورًا من غير ما تفضل النسخة القديمة متخزنة.

# v11-69 — 2026-09-14 — تنظيف تسجيلات المكالمات من التخزين + سؤال الاحتفاظ/الحذف

⚠️ اتنفذ بمساعدة Claude بناءً على ملاحظة المستخدم إن التسجيلات الصوتية
بتاخد مساحة تخزين دائمة (IndexedDB عبر ImageStore، زي صور الأجهزة
بالظبط) ومش بتتنضف لما يتحذف أمر الشغل المرفقة بيه.

- 🐛 **إصلاح تسريب مساحة تخزين**: `deleteRequestRecord`،
  `deleteAllRequests`، `deleteAllCustomers`، و`deleteAllDevices` كانوا
  بيمسحوا أمر الشغل من `localStorage` من غير ما يمسحوا التسجيل الصوتي
  المرفق بيه من IndexedDB — فكان بيفضل تايه وياخد مساحة للأبد. دلوقتي
  بيتمسح تلقائيًا (نفس منطق تنظيف صور الأجهزة الموجود جنبه بالظبط) عن
  طريق دالة مشتركة جديدة `cleanupRequestRecordings()` في
  `app-delete-tools.js`.
- 🆕 **سؤال الاحتفاظ/الحذف بعد إنشاء الأمر**: لما تتلحق مكالمة بأمر شغل
  (جديد أو مفتوح بالفعل) من `share-target.js`، النظام بيسأل فورًا
  "تحتفظ بالتسجيل جوه الأمر ولا تحذفه؟" — لو المستخدم اختار الحذف،
  بيتمسح من IndexedDB على طول قبل حتى ما يتسجل في الأمر، بدل ما يفضل
  مخزّن من غير داعي. مفيدة خصوصًا للي بيستخدم التسجيل بس وقت إنشاء
  الأمر (لمعرفة رقم التليفون/تفاصيل العطل) ومش محتاجه بعد كده.

# v11-68 — 2026-09-14 — إرجاع `id` لنفس القيمة القديمة بالظبط (بناءً على طلب المستخدم)

⚠️ اتنفذ بمساعدة Claude بناءً على طلب المستخدم: تراجع عن الـ `id` الجديد
(`/workshop-fanya-pwa` من v11-67) والرجوع لنفس القيمة القديمة بالظبط
(`./?app=workshop-mini`) اللي كانت مستخدمة قبل v11-66. المستخدم موضّح
إنه مش متأكد 100% إن المشكلة الأصلية (فتح التطبيق كصفحة عادية بدل تطبيق
مثبت) كانت سببها الـ `id` أصلًا — وإنها ممكن تكون بسبب إعداد "المتصفح
الافتراضي" في الجهاز (سامسونج براوزر) مش المانفست — وعايز يمشي في نفس
المسار اللي كان مستقر معاه، مع إنه في مرحلة تجربة بيمسح ويعيد تثبيت
التطبيق كتير ومش عايز أي احتكاك إضافي حاليًا.

- 🔄 `manifest.json`: `id` رجع لـ `./?app=workshop-mini` (بدل
  `/workshop-fanya-pwa`).
- ⚠️ **ملحوظة مهمة اتقالت للمستخدم**: القيمة دي هي نفسها اللي كانت موجودة
  في نسخة قديمة تانية (v11-65) لسه محتفظ بيها كـ"نسخة احتياطية مستقرة"
  على نفس الدومين — لو الاتنين (القديمة الاحتياطية والجديدة دي) واتثبتوا
  على نفس الدومين في نفس الوقت، ممكن يحصل تعارض هوية بينهم (نفس المشكلة
  الموثقة في v11-66) لأنهم هيبقوا بنفس الـ `id` بالظبط. المستخدم عارف
  الاحتمال ده وقرر يمشي عليه دلوقتي على أساس إنه يعدّله لو ظهرت مشكلة
  فعلية.
- رفع رقم النسخة لـ 11.68 في كل الصفحات، تسجيل الـ Service Worker، ورقم
  كاش `service-worker.js`.

# v11-67 — 2026-09-14 — إرجاع `id` في المانفست بقيمة مختلفة (مش نفس القديمة)

⚠️ اتنفذ بمساعدة Claude بناءً على طلب المستخدم: إرجاع حقل `"id"` في
`manifest.json` (كان اتشال في v11-66) عشان يمنع طلب Clear + إعادة تثبيت
كل ما يحصل تعديل مستقبلي في المانفست — المستخدم أكّد إن سبب شيله الأصلي
(مشكلة فتح التطبيق كصفحة عادية في متصفح تاني بدل كتطبيق مثبت) كان راجع
لإعداد "المتصفح الافتراضي" في الجهاز (سامسونج براوزر بدل كروم)، مش للـ
`id` نفسه، ومفيش مشروع تاني حاليًا بيستخدم نفس الدومين.

- 🆕 إضافة `"id": "/workshop-fanya-pwa"` للمانفست — قيمة مختلفة تمامًا عن
  القديمة (`./?app=workshop-mini`) اللي كانت بتسبب تعارض هوية WebAPK مع
  مشروع تاني على نفس الدومين، لتفادي تكرار نفس المشكلة.
- رفع رقم النسخة لـ 11.67 في كل الصفحات (`manifest.json?v=`)، تسجيل
  الـ Service Worker، ورقم كاش `service-worker.js`.
- ⚠️ **تعديل المانفست ده (زي أي تعديل مانفست) لازم إلغاء تثبيت وإعادة
  تثبيت التطبيق مرة واحدة بعد Clear لبيانات الموقع من كروم** — الفايدة
  من `id` الثابت بتبدأ تظهر بعد كده في أي تحديثات مانفست جاية (مش
  محتاجة تكرار الخطوة دي تاني).

# v11-66 — 2026-09-13 — 📞 دمج ميزة "مشاركة مكالمة ← أمر شغل" (مع كل التحسينات) + إصلاحين قديمين على هذه النسخة

⚠️ اتنفذ بمساعدة Claude بناءً على طلب المستخدم: دمج ميزة استقبال مكالمة
مشاركة من تطبيق تسجيل المكالمات (اللي اتطورت وتأكد استقرارها على نسخة
اختبار منفصلة، راجع نقاش سابق طويل عن استقرار WebAPK ومشكلة تعارض
الـ manifest id) على هذه النسخة (اللي كانت بلا الميزة دي لكن فيها تحسينات
تانية — زرار الرئيسية وتصحيح الوضع الداكن) — مع إضافة كل التحسينات
المتفق عليها على الميزة الأساسية. تحقق `node --check` على كل ملفات JS +
فحص بنية HTML للصفحات الجديدة + فحص صحة JSON للمانفست. من غير تشغيل
فعلي على جهاز حقيقي — لازم اختبار على الموبايل قبل الاعتماد الكامل.

**الأساس (زي ما كان متأكد منه في نسخة الاختبار):**
- 🆕 `manifest.json`: `share_target` (POST/multipart) يخلي "الورشة الفنية"
  تظهر في قايمة "مشاركة" لأي تطبيق بيشارك نص أو ملف صوت.
- 🐛 اتشال حقل `"id": "./?app=workshop-mini"` من المانفست — ده كان بيتسبب
  في عدم استقرار WebAPK (تعارض هوية بين مشاريع مختلفة على نفس الدومين)،
  اتأكد منه بالتشخيص الفعلي (`chrome://webapks`) في نقاش سابق.
- 🆕 `service-worker.js` + `share-store.js`: استقبال الـ POST الفعلي من
  نظام المشاركة وتمريره لصفحة استقبال عادية.
- 🐛 إعادة تطبيق إصلاح `requestAgeMs` (v11-62 سابقًا): عمر الأمر كان
  بيتجمّد غلط على تاريخ إكمال قديم لو الأمر اتعمله "مرتجع"/إعادة فتح —
  النسخة دي معندهاش الإصلاح ده أصلًا، فاتضاف تاني هنا.

**تحسينات جديدة على الميزة (بناءً على طلب المستخدم "كل التحسينات اللي قلناها"):**
- 🆕 **إلحاق بأمر مفتوح بدل التكرار**: لو رقم التليفون معروف وللعميل أمر
  شغل مفتوح بالفعل، الصفحة بتعرض اختيار "📎 ألحق بالأمر ده" بدل ما تنشئ
  أمر جديد مكرر لنفس المشكلة — بيلحق نص المكالمة الجديد بوصف العطل
  (بفاصل تاريخ) والتسجيل الصوتي (لو موجود) على نفس الأمر.
- 🆕 **أكتر من تسجيل صوتي لكل أمر**: الحقل بقى `callRecordings` (قائمة)
  بدل مرجع واحد — لو العميل كلّم أكتر من مرة على نفس العطل، كل التسجيلات
  بتتحفظ وتظهر مرقّمة في صفحة الأمر (`request.html`).
- 🆕 **"مكالمات معلّقة" (`pending-calls.html`)**: أي مكالمة مشاركة بتتحفظ
  فورًا في `localStorage` (`wf_pending_calls`) وقت وصولها، قبل حتى ما
  تتملى في الفورم — فلو قفلت الصفحة أو الموبايل من غير ما تكمّل، المكالمة
  مش بتضيع؛ هتلاقيها في قايمة جديدة على الداشبورد ("📞 مكالمات معلّقة")
  وتقدر تكمّلها أو تتجاهلها وقت ما تحب.
- رفع رقم الكاش لـ v11.66، ورقم `?v=` بالمانفست وتسجيل الـ Service
  Worker بكل الصفحات لـ 11.66.
- ⚠️ ملحوظة: زي أي مانفست تعديل، لازم إلغاء تثبيت وإعادة تثبيت التطبيق
  المثبت حاليًا (بعد Clear & reset لبيانات الموقع من كروم) عشان يشتغل
  صح — راجع الخطوات اللي اتفقنا عليها قبل كده.

# v11-64 — 2026-09-12 — 🏠 زرار "الرئيسية" في الشريط السفلي + تصحيح ألوان الوضع الداكن

⚠️ اتنفذ بمساعدة Claude، بناءً على طلبين من المستخدم: "عايز زرار الصفحة
الرئيسية أقدر أشوفه من أي مكان (مش لازم أطلع لفوق في قايمة طويلة عشانه)"،
و"لو لقيت أي حاجة عايزة تحسين في السمة ظبطها".

- 🏠 اتضاف تاب "الرئيسية" لأول عنصر في الشريط السفلي الثابت
  (`bottom-nav.js`) اللي أصلًا ظاهر في كل صفحات النظام فوق أي محتوى —
  فبقى الرجوع للصفحة الرئيسية متاح من غير ما تحتاج تسكرول لفوق خالص،
  من أي قائمة طويلة كانت (عملاء، أجهزة، أوامر...).
- 🎨 لقيت باگ حقيقي في السمة أثناء المراجعة: الشريط السفلي (`bottom-nav.js`)
  ونافذة البحث الشامل (`global-search.js`) كانوا بيثبّتوا ألوان الوضع
  الفاتح (خلفية بيضاء، حدود، نصوص رمادي فاتح...) بالقيمة الصريحة (hex)
  بدل ما يستخدموا متغيرات السمة (`--bg-elevated`, `--text-muted`,
  `--accent-text`, `--border`) زي باقي الواجهة — يعني في الوضع الداكن،
  الشريط السفلي وصندوق البحث كانوا هيفضلوا أبيض وسط صفحة داكنة بدل ما
  يتحولوا للداكن زي الباقي. اتصلح الاتنين ليستخدموا متغيرات السمة (مع
  قيمة احتياطية بنفس لون الوضع الفاتح القديم لو المتغير مش موجود لأي
  سبب)، وأضيف تظليل مناسب لحالة "التاب النشط" في الوضع الداكن.
- تحقق `node --check` على الملفين المعدّلين. من غير تشغيل فعلي في متصفح
  حقيقي أو مقارنة بصرية مباشرة بين الوضعين — يستاهل تأكيد بصري بسيط بعد
  التحديث لو ينفع.
- رفع رقم الكاش لـ v11.64.

# v11-63 — 2026-09-12 — ✍️ إضافة أكواد أعطال أسرع (حفظ متتالي + لصق دفعة)

⚠️ اتنفذ بمساعدة Claude، بناءً على طلب المستخدم بعد v11-62: "فيه إمكانية
نضيف أكواد بسهولة أكتر؟". اتفقنا على حاجتين:

- 💾 زرار "حفظ وإضافة كود تاني" في فورم إضافة كود جديد (`faultcodes.html`
  ← `app-fault-codes.js`): بيحفظ الكود الحالي من غير ما يوديك لصفحته،
  وبيسيب النوع والماركة زي ما هما ويفضّي بس حقول الكود/الوصف/السبب/الحل
  عشان تكتب الكود اللي بعده على طول لنفس الماركة. مش ظاهر أثناء تعديل
  كود موجود بالفعل (منطقي بس وقت الإضافة).
- 📋 قسم "لصق دفعة أكواد" جديد فوق الصفحة: تختار النوع والماركة مرة واحدة،
  تلصق سطر لكل كود بالشكل `الكود | وصف قصير | السبب | الحل | ملاحظة`،
  تدوس "معاينة" فيوريك قايمة بكل سطر (وواضح فيها أي سطر ناقص أو مكرر —
  سواء مكرر مع أكواد محفوظة قبل كده أو مكرر مع سطر تاني في نفس اللصقة)
  قبل أي حفظ فعلي، وبعد المراجعة تدوس تأكيد فيتحفظوا كلهم مرة واحدة.
- تحقق `node --check` على الملف المعدّل، وفحص توازن الوسوم في
  `faultcodes.html`. من غير تشغيل فعلي في متصفح حقيقي.
- رفع رقم الكاش لـ v11.63.

# v11-62 — 2026-09-12 — 🗑️ حذف جماعي لأكواد الأعطال (الكل أو حسب النوع/الماركة)

⚠️ اتنفذ بمساعدة Claude، بناءً على طلب المستخدم: "قسم الأكواد عايز فيه
زرار زي إعدادات أقدر أمسح بيه كل الأكواد مرة واحدة، أو أحذف/أعدل أي
بند أو تصنيف". التعديل/الحذف الفردي لكود واحد كان موجود بالفعل (من
صفحة الكود نفسه)، فالإضافة هنا اقتصرت على الحذف الجماعي:

- 🗑️ زرار "حذف كل الأكواد" في أعلى صفحة `faultcodes.html` — بيمسح كل
  أكواد الأعطال المسجّلة (كل الأنواع والماركات) بعد تأكيد مزدوج، بنفس
  أسلوب أزرار الحذف الجماعي الموجودة أصلًا للعملاء/الأجهزة/الأوامر في
  `app-delete-tools.js`.
- 🗑️ زرار "حذف كل أكواد هذا الفلتر" بجوار فلاتر النوع/الماركة فوق
  القائمة — بيمسح بس الأكواد المطابقة للفلتر المختار حاليًا (مثلًا كل
  أكواد "توشيبا" أو كل أكواد "غسالات")، مفيد لمسح دفعة استوردتها غلط من
  غير ما تلمس باقي الأكواد. لازم تختار نوع أو ماركة الأول (منعًا لمسح
  الكل بالغلط من الزرار ده بالذات).
- الدالتين الجديدتين (`deleteAllFaultCodes`, `deleteFilteredFaultCodes`)
  في `app-fault-codes.js`.
- تحقق `node --check` على الملف المعدّل، وفحص توازن الوسوم في
  `faultcodes.html`. من غير تشغيل فعلي في متصفح حقيقي.
- رفع رقم الكاش لـ v11.62.

# v11-58 — 2026-09-08 — 🎨 شريط لون فوق كارت القطعة في المخزن (زي كارت الأمر)

⚠️ اتنفذ بمساعدة Claude. المستخدم بعت صورة من شاشة الأوامر الحقيقية
وأكّد إن شريط اللون فوق الكارت شكله كويس، وطلب نفس الحاجة في المخزن.
في v11-57 كان لون التصنيف موجود بس على أيقونة القطعة والشارة، من غير
شريط فوق الكارت نفسه. اتصلح: كارت القطعة في `inventory.html` (عرض
"كروت") بقى له شريط لون 4px فوقه بلون تصنيفه، بنفس أسلوب شريط عمر
الأمر بالضبط.

- تحقق `node --check`. من غير تشغيل فعلي في متصفح حقيقي.
- رفع رقم الكاش لـ v11.58.

# v11-57 — 2026-09-08 — 🎨 لون ثابت لكل تصنيف قطع غيار في المخزن

⚠️ اتنفذ بمساعدة Claude، بناءً على طلب المستخدم. فكرة مختلفة عن تلوين
عمر الأمر (اللي بيتغيّر تلقائي مع الوقت) — هنا اللون **ثابت** حسب
التصنيف نفسه (غسالات / تلاجات وفريزرات / تكييف / سخانات...):

- 📦 كل تصنيف في المخزن (`inventory.html`) بقى له لون مميز وثابت:
  دايرة الأيقونة، شارة التصنيف، وشريط لون فوق تايل التصنيف في ملخص
  المخزن، وفي عرض الجدول كمان (نقطة ملونة جنب اسم التصنيف).
  الألوان بتتحسب أوتوماتيك من اسم التصنيف نفسه (8 ألوان بتتكرر لو
  التصنيفات أكتر من 8)، فأي تصنيف جديد يضيفه المستخدم من ⚙️ الإعدادات
  ياخد لون تلقائي من غير أي إعداد إضافي.
- 📦 صفحة القطعة المفردة (`part.html`) بقى فيها نفس شارة التصنيف
  الملوّنة بدل النص العادي.
- الفكرة دي منفصلة تمامًا عن ترميز عمر الأمر (v11-55/56) ومفيش تعارض
  بينهم — كل واحد بيستخدم كلاس CSS مختلف (`age-b*` مقابل `cat-c*`).
- تحقق `node --check` على كل ملفات الـ JS. من غير تشغيل فعلي في متصفح حقيقي.
- رفع رقم الكاش لـ v11.57.

# v11-56 — 2026-09-08 — 🎨 تعميم لون عمر الأمر على العملاء والأجهزة وخط السير

⚠️ اتنفذ بمساعدة Claude، بناءً على طلب المستخدم إنه نفس فكرة تلوين
عمر الأمر (v11-55) متتحصرش في قايمة أوامر الشغل بس، وتتعمم على أي
مكان بيظهر فيه عميل/جهاز ليه أمر شغل مفتوح:

- 👤 قايمة العملاء (`customers.html`) وبروفايل العميل: كل عميل/جهاز
  ليه أمر مفتوح بقى بياخد لون أسوأ (أقدم) أمر عنده — نفس الشريط
  والشارة الملوّنة اللي في أوامر الشغل.
- 🔧 قايمة الأجهزة (`devices.html`) وبروفايل الجهاز (سجل أوامر الشغل):
  نفس الفكرة.
- 🗺️ خط السير (`route.html`): كل توقف — سواء الكارت الكامل ("الدور
  عليك دلوقتي") أو السطر المختصر للباقي — بقى فيه شارة عمر الأمر
  الملوّنة برضه.
- الحساب دايمًا مبني على نفس الدالة الموحّدة `requestAgeInfo` (ولمجموعة
  أوامر: `worstRequestAgeInfo` بياخد أسوأ/أقدم أمر مفتوح)، ونفس عتبة
  "⏳ تنبيه الأوامر القديمة" اللي المستخدم بيتحكم فيها من الإعدادات —
  فلو غيّرت الرقم هناك، الألوان في كل الصفحات دي بتتحدّث تلقائي.
- تحقق `node --check` على كل ملفات الـ JS. من غير تشغيل فعلي في متصفح حقيقي.
- رفع رقم الكاش لـ v11.56.

# v11-55 — 2026-09-08 — 🎨 تبسيط ألوان عمر الأمر + تنبيه قابل للتحكم للأوامر القديمة

⚠️ اتنفذ بمساعدة Claude، استكمالًا لطلب المستخدم. أثناء المراجعة اتضح
إن ترميز اللون اللي اتضاف في v11-54 كان بيتحط في `app-requests.js`
بس، وde الملف فعليًا **مش المستخدم في صفحة أوامر الشغل** — فيه نسخة
تانية بتستبدله (`renderRequests` في `workshop-mini-simple-ui.js` عن
طريق `defineOverride`)، فاللون في الواقع ما كانش ظاهر للمستخدم خالص.
اتصلح هنا بوضع التلوين في المكان الصحيح (الكارت `.simple-record`)،
غير كده اتنفذ برضه:

- 🎨 قللنا الألوان من 6 لـ 3 بس (زي ما المستخدم طلب): 🟢 جديد، 🟡
  متوسط، 🔴 قديم — بدل الشرايح الست.
- ⚙️ ضفنا إعداد جديد في ⚙️ الإعدادات ← "⏳ تنبيه الأوامر القديمة"،
  المستخدم بيتحكم فيه بنفسه في عدد الأيام (افتراضيًا 7). نفس الرقم ده
  بيتحكم في: (1) امتى يتلوّن الأمر أحمر في القايمة، و(2) امتى يدخل
  عداد "🔥 يحتاج انتباه" في الشاشة الرئيسية.
- 📊 الشاشة الرئيسية بقى فيها عداد جديد "⏳ قديمة (X+ يوم)" بيودّي
  مباشرة لقايمة الأوامر القديمة دي فلترة (`requests.html?bucket=stale`).
  لاحظ الفرق عن العداد "⚠️ متأخرة" الموجود من قبل: ده مبني على تاريخ
  *زيارة* فاتت، أما "قديمة" فمبني على *عمر الأمر منذ تسجيله* بغض النظر
  عن موعد الزيارة.
- الأوامر المكتملة والملغية والمقفولة برّه الحساب في كل ده، زي ما هو
  متفَق عليه، وقاعدة "الأولوية مش موجودة في دورة أمر الشغل" لسه زي ما
  هي (ده مجرد لون/تنبيه تلقائي مش حقل بيتحرّر يدويًا).
- تحقق `node --check` على كل ملفات الـ JS. من غير تشغيل فعلي في متصفح حقيقي.
- رفع رقم الكاش لـ v11.55.

# v11-54 — 2026-09-08 — 🎨 ترميز لوني لعمر أوامر الشغل في القايمة

⚠️ اتنفذ بمساعدة Claude، بناءً على طلب المستخدم إنه لما يفتح كل أوامر
الشغل مع بعض يصعب عليه يفرّق بصريًا مين جديد ومين قاعد من زمان محتاج
تنفيذ الأول. الأولوية اليدوية فعليًا اتشالت من دورة أمر الشغل
(WORK_ORDER_LIFECYCLE_APPROVED.md) ولسه كده — الإضافة دي لون إرشادي
بيتحسب أوتوماتيك من تاريخ التسجيل، مش حقل بيتعدّل يدويًا، ومفيش أي
تغيير في البيانات المخزّنة أو منطق الحالات. تحقق `node --check` على
`app-shared.js` و`app-requests.js`. من غير تشغيل فعلي في متصفح حقيقي.

- ✅ كل كارت أمر شغل (في صفحة أوامر الشغل) بقى ليه شريط لون أعلاه +
  شارة صغيرة بعدد الأيام، بيتغير كل يومين تقريبًا من أخضر (جديد) لحد
  أحمر (10 أيام فأكتر): 🟢 يوم أو أقل، 🟡 يومين-3، 🟠 4-5، 🟠 6-7،
  🔴 8-9، 🔴 10+.
- الترميز ده بيظهر بس على الأوامر المفتوحة (جديد / جاري التنفيذ) —
  المكتمل والملغي مالهمش لون عشان مش محتاجين متابعة عمر.
  العمر بيتحسب من تاريخ تسجيل الأمر (نفس منطق `requestCreatedDate`
  المستخدم في صفحة الأمر 360° لحساب "تسجيل البلاغ").
- ✅ ظهر شريط توضيحي (Legend) فوق قايمة الأوامر يشرح كل لون بيعني إيه.
- رفع رقم الكاش لـ v11.54.

# v11-53 — 2026-09-08 — 🐛 إصلاح خطأ جوهري: عناصر الإعدادات كانت بتختفي بعد الإضافة

⚠️ اتنفذ بمساعدة Claude، بعد ما المستخدم بعت صورة توضح إن قسمي
"wallets" و"walletCategories" في الإعدادات ظاهرين باسمهم الإنجليزي
الخام مش العنوان العربي، وإن أي عنصر بيتضاف (جرّب "بطاطس" و"عنب"
كاختبار) بيختفي وميظهرش في باقي الشاشات. تحقق `node --check` على كل
ملفات JS المتأثرة + محاكاة مباشرة بـ node للتأكد من ترتيب الوسائط
الصحيح. من غير تشغيل فعلي في متصفح حقيقي.

- 🐛 **السبب الجذري**: الدوال `listEditorHtml(title,key,icon)` و
  `inlineListEditorHtml(title,key,icon)` بتتنادى من مصفوفات مكتوبة
  بترتيب `[key,title,icon]` (عكس ترتيب معاملات الدالة)، فكان بيتبعت
  اسم المفتاح الإنجليزي مكان العنوان المعروض (عشان كده يظهر
  "wallets" بدل "الحسابات")، والعنوان العربي (المفروض يبقى نص وصفي
  بس) بيتبعت مكان مفتاح البيانات الفعلي — فأي إضافة كانت بتتسجل تحت
  مفتاح إعدادات وهمي باسم عربي طويل، منفصل تمامًا عن `wallets` الحقيقي
  اللي باقي الشاشات بتقرأ منه. الخطأ ده كان موجود من نسخة التطبيق
  الأصلية (قبل أي تعديل من Claude) في 4 قوائم (أماكن التنفيذ، حالات
  الدفع، وحدات القياس، أنواع العناوين)، وانتقل بالغلط لأي قايمة
  جديدة اتضافت بعد كده بنفس النمط (الحسابات، التصنيف، نوع مصروف
  التشغيل/الشخصي).
- ✅ **الإصلاح**: تصحيح ترتيب كل المصفوفات الـ8 المتأثرة لتطابق ترتيب
  معاملات الدالة فعليًا.
- 🩹 **ترحيل بيانات تلقائي (schema v5 → v6)**: أي عنصر كان اتسجل
  بالغلط تحت الاسم الخاطئ (زي "بطاطس" و"عنب" في تجربة المستخدم)
  بينتقل تلقائيًا أول ما يفتح التطبيق لمكانه الصحيح (بدون تكرار لو
  موجود بالفعل، ومن غير ما يتمسح أي حاجة). لو شفت عنصر تجربة زي ده
  ظاهر دلوقتي في القايمة الصحيحة، احذفه عادي — ده متوقع ومعناه إن
  الترحيل شغال.
- رفع رقم الكاش لـ v11.53.


# v11-52 — 2026-09-07 — تجميع "التصنيف" و"نوع المصروف" جوه إعدادات الحسابات

⚠️ اتنفذ بمساعدة Claude، بناءً على ملاحظة المستخدم إنه عايز يتحكم في
"التصنيف" و"نوع المصروف" (اللي بيشوفهم وهو بيسجل حركة محفظة) من
الإعدادات. القوائم دي كانت أصلًا قابلة للتعديل بالكامل، بس متفرقة في
مكان بعيد عن قسم الحسابات نفسه — التعديل ده تنظيمي/مكاني بحت، من
غير أي تغيير في البيانات المخزّنة. تحقق `node --check` على
`app-settings.js` و`wallets.js`. من غير تشغيل فعلي في متصفح حقيقي.

- 🔧 نقل قوائم "نوع مصروف التشغيل" (`expenseCategories`) و"نوع
  المصروف الشخصي" (`personalExpenseCategories`) من قسم "دورة حالات
  أمر الشغل والقوائم" (مكانهم غير المتوقع) إلى قسم "💳 الحسابات" في
  الإعدادات مباشرة، جنب "التصنيف" (`walletCategories`) و"الحسابات"
  (`wallets`) — دلوقتي التصنيف ونوعه الفرعي في نفس المكان بالظبط.
- ✨ زرار "⚙️" صغير جنب حقل "التصنيف" وحقل "نوع المصروف" في فورم
  تسجيل حركة محفظة (صفحة الحسابات وصفحة أي محفظة بعينها) بيودّي
  على طول لقسم الحسابات في الإعدادات.
- رفع رقم الكاش لـ v11.52.


# v11-51 — 2026-09-07 — قسم جديد مستقل: أكواد أعطال الأجهزة

⚠️ اتنفذ بمساعدة Claude، بناءً على طلب المستخدم بعد مراجعة شاملة
للنظام لتقييم إمكانية إضافة أقسام مستقلة غير متصلة بالأقسام
الأساسية (عملاء/أجهزة/أوامر شغل). تحقق `node --check` على كل ملفات
JS المتأثرة. من غير تشغيل فعلي في متصفح حقيقي.

- ✨ **قسم "🧯 أكواد أعطال الأجهزة" (`faultcodes.html` + `faultcode.html`
  + `app-fault-codes.js`)**: مرجع مستقل تمامًا عن العملاء/الأجهزة/أوامر
  الشغل — كل سجل فيه كود العطل + نوع الجهاز + الماركة (الاتنين اختياريين،
  فاضي = "عام" لكل الأنواع/الماركات) + وصف قصير + السبب المحتمل + خطوات
  الفحص/الحل + ملاحظات + اسم الفني اللي أضافه. كشف تكرار بنفس منطق
  المخزن بالظبط (نفس الكود لنفس النوع/الماركة ميتسجلش مرتين). قابل
  للوصول من كارت جديد في الشاشة الرئيسية تحت قسم "📚 مرجع فني (مستقل)"،
  ومندمج في البحث الشامل (فئة جديدة "faultcode").
- ✨ **مشاركة الأكواد مع فنيين تانيين**: زر تصدير مستقل (`exportFaultCodesOnly`)
  بيطلع ملف JSON فيه الأكواد بس (من غير أي بيانات عملاء أو مالية)،
  وزر استيراد (`importFaultCodesFile`) بيدمج الملف ده مع أكواد الفني
  التاني من غير ما يمسح اللي عنده، ومن غير تكرار لنفس الكود. منفصل
  تمامًا عن النسخة الاحتياطية الكاملة (اللي لسه بتشمل كل البيانات
  الحساسة زي العادة).
- 🔧 مفتاح تخزين جديد `K.fc` (`wf_fault_codes`) — بينضم تلقائيًا للنسخة
  الاحتياطية الكاملة (لأنها بتاخد كل مفاتيح `K`)، ومستبعد عمدًا من
  "حذف كل البيانات التشغيلية" (لأنه مرجع/قاعدة معرفة زي أنواع
  الأجهزة والماركات، مش بيانات عميل).
- رفع رقم الكاش لـ v11.51.


# v11-50 — 2026-09-07 — تفصيل قسم الحسابات + حد أقصى لحساب إنستاباي

⚠️ اتنفذ بمساعدة Claude، بناءً على طلب المستخدم. تحقق `node --check`
على كل ملفات JS المتأثرة (`shared-data.js`, `wallets.js`,
`app-dashboard-reports.js`, `app-settings.js`). من غير تشغيل فعلي
في متصفح حقيقي.

- ✨ **حد أقصى اختياري لأي حساب** (`s.walletCaps`): مفيد لحساب زي
  إنستاباي اللي هو في الأصل جزء من حساب بنكي شخصي مش عايز يتسجل هنا
  بكامل رصيده — تحط رقم تقريبي كحد أقصى من ⚙️ الإعدادات ← الحسابات
  ← 🔒 حد أقصى اختياري لبعض الحسابات، وهو ده اللي بيدخل في رصيد
  المحفظة المعروض وفي "إجمالي أرصدة كل الحسابات" فوق صفحة الحسابات،
  حتى لو الحركات الفعلية المسجلة جمعت لرقم أعلى. كشف حركات المحفظة
  نفسه يفضل يعرض كل الحركات الحقيقية بدون أي تعديل، مع ملحوظة توضح
  الفرق بين الرصيد الفعلي والمعروض لو فيه حد مضبوط.
- ✨ **تصنيفات فرعية للمصاريف الشخصية** (`s.personalExpenseCategories`،
  زي مواصلات/أكل وشرب/متفرقات...): "مصروف شخصي" بقى قابل للتفصيل هو
  كمان بنفس آلية "مصروف تشغيل" بالظبط، وقابل للتعديل بالكامل
  (إضافة/تعديل/حذف/ترتيب) من ⚙️ الإعدادات. صافي مكسب الورشة لسه
  بيتحسب من مصاريف التشغيل فقط زي الأول، مفيش تأثير على حساب الربح.
- ✨ **إحصائيات صرف جديدة في صفحة الحسابات**: كارت "📊 إحصائيات الصرف"
  أعلى الصفحة بيعرض إجمالي المصروف (تشغيل + شخصي) للشهر الحالي وأكتر
  5 حاجات بيتصرف عليها مرتبة تنازليًا، بشكل مستقل عن فلتر
  الشهر/الأسبوع تحت. قسم "📈 كشف الحساب" (المفصّل، بفلتر الفترة) بقى
  كمان بيجمع مصاريف التشغيل والشخصي مع بعض بدل ما يقتصر على التشغيل
  بس، ومظهر 🏆 أعلى بند صرف في الفترة المختارة.
- 🏷️ رصيد المحفظة في صفحتها الخاصة (`wallet.html?type=wallet...`)
  بقى بيحترم الحد الأقصى لو متحدد ليها (كان بيحسب من الحركات مباشرة
  من غير أي حد قبل كده).
- 🔧 حذف أو تعديل اسم أي حساب من الإعدادات بقى بينضّف/ينقل الحد
  الأقصى الخاص بيه معاه تلقائيًا، بدل ما يفضل معلّق باسم قديم.
- رفع رقم الكاش لـ v11.50.


# v11-49 — 2026-09-07 — تذكير النسخة الاحتياطية

⚠️ اتنفذ بمساعدة Claude، بناءً على طلب المستخدم بعد مراجعة شاملة
كشفت إن مفيش أي تذكير بعمل نسخة احتياطية رغم إن كل بيانات النظام
محفوظة محليًا بس (لا يوجد مزامنة سحابية). تحقق `node --check` على
كل الملفات المتأثرة، وتأكيد إن `backup-reminder.js` مضاف لـ
`CORE_FILES` ومطابقة كاملة بين كل ملفات JS/CSS/HTML الفعلية وقائمة
كاش الأوفلاين (سكريبت تحقّق آلي). من غير تشغيل فعلي في متصفح حقيقي.

- ✨ **بانر تذكير في الصفحة الرئيسية** (`backup-reminder.js`): بيظهر
  لو فات 14 يوم من غير نسخة احتياطية (أو معملتش نسخة أبدًا)، مع زرار
  مباشر لصفحة النسخ الاحتياطي وزرار "تذكير بعد 3 أيام" لو مش وقتها
  دلوقتي.
- 🔔 **تنبيه دوري** (لمن فعّل الإشعارات): اتضاف كنوع رابع جنب مواعيد
  اليوم/المتأخر/المخزون المنخفض في `app-notifications-bootstrap.js`.
- ℹ️ **سطر معلومة في الإعدادات**: صفحة النسخة الاحتياطية بقى فيها
  "آخر نسخة احتياطية: من كذا يوم" تحت شرح الميزة مباشرة.
- 🔧 كل تصدير ناجح (`backupAllData`) أو استرجاع لملف فيه تاريخ تصدير
  (`restoreBackupFile`) بيسجّل `wf_last_backup_at` في localStorage —
  مفتاح جديد وحيد، من غير أي تغيير في بنية بيانات النظام الحالية.
- رفع رقم الكاش لـ v11.49.


# v11-48.2 — 2026-09-07 — مراجعة شاملة: إصلاح ملف ناقص من كاش الأوفلاين + تنظيف كود ميت

⚠️ اتنفذ بمساعدة Claude، بناءً على طلب مراجعة شاملة للنسخة (مشاكل
معمارية/أخطاء/كود ميت). فحص آلي كامل: مطابقة كل ملف JS/CSS متضاف في
صفحات HTML مع `CORE_FILES` في الـ Service Worker، فحص كل الدوال
المعرّفة على المستوى العام (279 دالة) ضد كل استدعاء لها في المشروع
كله، `node --check` على كل الـ31 ملف JS بعد كل حذف، وتأكيد توازن
الأقواس في `style.css`. من غير تشغيل فعلي في متصفح حقيقي.

- 🐛 **إصلاح:** `bottom-nav.js` (شريط التنقل السفلي، متحمّل في كل
  الـ18 صفحة) كان ناقص من `CORE_FILES` في `service-worker.js` —
  يعني أول تحميل أوفلاين لصفحة معينة ممكن يفشل يجيب الشريط. اتضاف
  للقائمة.
- 🗑️ **حذف كود ميت** (متعرّف ومالوش أي استدعاء في المشروع كله):
  `moveVillage`/`moveBrand`/`movePartCategory` (نظام ترتيب قديم
  بالأسهم، اتستبدل بإدخال رقم الترتيب `setListPosition`/
  `setVillagePosition`)، `removeWalletTxForRef` (منطقها بقى جوه
  `upsertWalletTxForRef` نفسها)، `bulkPriceTargets` (فلترة مكررة
  يدويًا في `applyBulkPriceChange`)، `taskDateTime`،
  `requestExecutionMs`، `upsertTreasuryEntry`، `syncTreasuryForExpense`.
  الدوال المشابهة اللي اتأكد إنها بتتنادى فعليًا كـ"نقاط ربط" مقصودة
  (`removeTreasuryEntry`, `syncTreasuryForOrderDeposit`,
  `syncTreasuryForOrderClose` — الخزنة مستقلة عمدًا) اتسابت زي ما هي.
- 🔧 رفع رقم النسخة في كل أماكن الكاش لـ v11.48.2.

# v11-48.1 — 2026-09-07 — إصلاح فلاش خانة البحث عند تحميل الصفحات

⚠️ اتنفذ بمساعدة Claude، بعد بلاغ من المستخدم إن خانة البحث بتظهر
للحظة أعلى الصفحة عند التحميل وبعدين تختفي. تحقق `node --check`
على `theme.js` وتأكيد صحة بنية الـ18 صفحة HTML بعد التعديل.

- 🐛 **السبب:** `theme.js` (اللي اتضاف في v11-48) كان متحمّل كسكريبت
  خارجي *قبل* رابط `style.css` في كل صفحة، وده بيأخر وصول المتصفح
  لرابط الأنماط (لازم يجيب وينفّذ السكريبت الأول)، فبتظهر لحظة
  "Flash of Unstyled Content" — أي عنصر زي خانة البحث بيترسم بشكله
  الخام (بعرض كامل وفي أول الصفحة) لحد ما `style.css` يوصل ويظبطه
  في مكانه الصح.
- ✅ **الحل:** فصلت الجزء اللي لازم يشتغل فورًا (تطبيق الوضع الداكن
  المحفوظ) لسطر واحد مضمّن (inline) في `<head>` — بينفّذ فورًا من
  غير أي طلب شبكة فبيبقى تأخيره صفر تقريبًا. وباقي `theme.js`
  (دالة `toggleTheme`) بقى بيتحمّل بخاصية `defer` عشان ميعطلش رسم
  الصفحة خالص.
- 🔧 رفع رقم الكاش لـ `v11.48.1` في كل الأماكن عشان الإصلاح يوصل
  للأجهزة اللي مثبتة عليها نسخة v11.48 بالفعل كـ PWA.

# v11-48 — 2026-09-07 — تحديث شامل للواجهة الرئيسية: وضع داكن + إعادة تنظيم

⚠️ اتنفذ بمساعدة Claude. تحقق `node --check` على كل ملفات الـ JS
المتأثرة (`app-dashboard-reports.js`، `theme.js`، `app-notifications-bootstrap.js`،
`service-worker.js`) + تحقق أن كل صفحات الـ HTML الـ18 لسه صحيحة
البنية بعد التعديل + مراجعة يدوية لكل الألوان المستبدلة في `style.css`
(تصنيف كل لون كخلفية محايدة/حد/نص فاتح قبل تحويله لمتغير، مع استثناء
الشارات والألوان الدلالية الملوّنة عشان متتلخبطش) + تأكيد توازن
الأقواس في `style.css`. من غير تشغيل فعلي في متصفح حقيقي، فلازم
يتجرب يدويًا خصوصًا في الوضع الداكن على كل الصفحات.

- ✨ **وضع داكن/فاتح لكل صفحات النظام** (مش بس الرئيسية): ملف جديد
  `theme.js` بيحفظ اختيار المستخدم في `localStorage` (`wf_theme`)
  ويطبّقه فورًا قبل الرسم عشان مفيش وميض. زرار 🌙/☀️ في هيدر
  الرئيسية، وفي شريط علوي جديد (`.page-topbar`) جنب رابط "← الرئيسية"
  في كل صفحة فرعية.
- 🎨 `style.css`: تحويل الألوان الأساسية (الخلفيات، الحدود، النصوص،
  لون التمييز الأزرق الغامق، ألوان النجاح/الخطر/التحذير) لمتغيرات
  CSS (`--bg`, `--bg-elevated`, `--bg-sunken`, `--border`, `--text`,
  `--text-muted`, `--accent-text`, `--danger-text`, `--success-text`,
  `--warn-text`) مع قيم مختلفة تحت `[data-theme="dark"]`. الشارات
  والتنبيهات ذات الخلفية اللونية الفاتحة (زي `.notice`, `.quick-add`)
  اتثبّت لها لون نص غامق صريح في الوضع الداكن عشان تفضل مقروءة.
  الطباعة (`@media print`) بتفضل فاتحة دايمًا بغض النظر عن الوضع
  المختار.
- 📊 إعادة تنظيم لوحة التحكم في الرئيسية (`renderDash`): الأرقام
  بقت مجمّعة في 4 مجموعات بعناوين — **🔥 يحتاج انتباه** (متأخر/انتظار
  قطع/مخزون منخفض، وبتظهر بس لو فيه فعلًا حاجة عاجلة)، **📅 اليوم**،
  **💰 المالية**، **👥 عام** — بدل ما تكون كل الأرقام في شبكة واحدة
  بلا ترتيب. "متأخرة" و"إيراد الشهر" بقى ليهم تمييز بصري (لون تحذيري
  / تدرّج بارز).
- 🗂️ إعادة تنظيم شبكة الأقسام في الرئيسية لمجموعات بعناوين:
  التشغيل اليومي، قاعدة البيانات، الأموال، الإدارة. وضفت كارت
  "المهام" اللي كان ناقص من الشبكة أصلًا (الصفحة كانت موجودة ومتاحة
  بس من غير رابط مباشر من الرئيسية).
- 🌅 تحية بالوقت + تاريخ اليوم أعلى الرئيسية، وخط عام أنضف (system
  font stack بدل Arial) مع تحسينات بصرية بسيطة على الهيدر.
- 🔧 رفع رقم النسخة في كل أماكن الكاش (`manifest.json?v=11.48`،
  `CACHE_NAME=workshop-v11-48`، `service-worker.js?v=11.48`) وإضافة
  `theme.js` لقائمة `CORE_FILES` في الـ Service Worker. من غير أي
  تغيير في بنية بيانات الـ localStorage الحالية.

# v11-47.1 — 2026-09-05 — مراجعة سريعة قبل السيرفر: إصلاح CSS ناقص

⚠️ اتنفذ بمساعدة Claude. مراجعة شاملة اتعملت على المشروع كله: فحص
`node --check` لكل ملفات الـ JS (43 ملف)، صحة `manifest.json`، كل
روابط src/href في صفحات الـ HTML بتشاور على ملفات موجودة فعلًا، كل
ملفات CORE_FILES في الـ Service Worker موجودة، كل صفحات الـ HTML
متضافة في كاش الأوفلاين، وعدم وجود بقايا من رقم النسخة القديم. من
غير تشغيل فعلي في متصفح حقيقي، فلازم يتجرب يدويًا.

- 🐛 لقيت حاجة واحدة ناقصة فعليًا: لما اتضاف زرار الطباعة/المشاركة
  للمهام/الحسابات/تفاصيل الحساب/الخزنة (v11-45)، اتضاف `print-share.js`
  بس اتنسى `print-share.css` — يعني الزرار كان هيشتغل لكن يطلع من
  غير الشكل (تنسيق) الصحيح. اتضاف الرابط الناقص في الصفحات الأربعة.
- ✅ كل باقي الفحوصات طلعت سليمة من غير أي مشكلة.

# v11-47 — 2026-09-05 — توحيد رقم الإصدار في كل أماكن الكاش (قبل النشر)

⚠️ اتنفذ بمساعدة Claude، تحقق `node --check` على ملفات الـ JS المتأثرة
+ تحقق إن `manifest.json` لسه JSON صحيح بعد التعديل + تأكيد يدوي إن كل
إشارة للأيقونات القديمة اتحدّثت، من غير تشغيل فعلي في متصفح حقيقي.
لازم يتجرب بعد الرفع مباشرة: افتح الموقع، امسح الكاش القديم من إعدادات
المتصفح مرة واحدة لو كنت مجرّبه قبل كده على نفس الجهاز، وتأكد إن
الأيقونة والعنوان ظاهرين صح وإن التطبيق شغال أوفلاين زي العادة.

- 🔧 كان فيه 4 صيغ لرقم الإصدار مش متزامنة مع بعضها: `CACHE_NAME` في
  الـ Service Worker (`workshop-v11-46`)، رابط تسجيل الـ Service Worker
  نفسه (`?v=11.8.1`)، رابط `manifest.json` في كل صفحة (`?v=11.4.1`)،
  وأسماء ملفات الأيقونات (`icon-192-v11-4-1.png` / `icon-512-...`).
  اتوحّدوا كلهم دلوقتي في رقم واحد: **v11-47**.
- 📄 اتغيّر اسم ملفي الأيقونة فعليًا (`icon-192-v11-47.png` /
  `icon-512-v11-47.png`) واتحدّثت كل الإشارات ليهم في: `manifest.json`،
  `service-worker.js` (قائمة الكاش الأساسية + أيقونات الإشعارات
  التلاتة)، وكل صفحات HTML (favicon + apple-touch-icon + شعار
  الرئيسية).
- 🎯 الهدف: ضمان إن المتصفح هياخد بال بالتحديثات الجديدة (خصوصًا كل
  التعديلات اللي اتعملت في الدفعات السابقة v11-43 لحد v11-45) بدل ما
  يفضل شايف نسخة قديمة من أي ملف متخزن في الكاش.
- من هنا وطالع: أي نشرة تعديلات جديدة على السيرفر، لازم رقم الإصدار
  ده يترفع في نفس الأماكن الأربعة مع بعض عشان يفضلوا متزامنين.
- لم يتغيّر أي منطق أو بيانات — رقم إصدار وأسماء ملفات فقط.

# v11-45 — 2026-09-05 — توحيد الأيقونات المتضاربة + تعميم زرار الطباعة/المشاركة على كل الأقسام

⚠️ اتنفذ بمساعدة Claude، تحقق `node --check` على كل ملف JS اتعدل +
فحص يدوي إن عدد وسوم `<script>` متوازن في كل صفحة HTML اتعدلت، من
غير تشغيل فعلي في متصفح حقيقي. لازم يتجرب يدويًا قبل النشر: خصوصًا
زرار 🖨️/↗️ الجديد في المهام/الحسابات/تفاصيل الحساب/الخزنة.

- 🎨 **فحص شامل لكل الأيقونات (الإيموجي) المستخدمة في النظام** قدام
  نفس الكلمة (تعديل، حذف، بحث، حفظ، إلغاء، مرتجع، الرئيسية...):
  لقيت أيقونة واحدة مختلفة عن الباقي في صفحة المهام — خانة البحث
  كانت 🔎 بدل 🔍 المستخدمة في كل باقي الصفحات (العملاء/الأجهزة/
  المخزن/الأوامر). باقي الأيقونات كانت متوحدة أصلًا من غير أي تعارض.
- 🎨 **تعميم زرار الطباعة/المشاركة (🖨️/↗️) على الأقسام اللي ماكانش
  فيها خالص**: كان موجود بس في العملاء/الأجهزة/المخزن/أوامر الشغل.
  دلوقتي بقى موجود كمان في:
  - 📋 المهام والمتابعة (زرار على عنوان قسم "📊 المتابعة").
  - 💳 الحسابات (زرار على عنوان الصفحة) وصفحة تفاصيل كل حساب/محفظة
    على حدة (زرار جنب اسم الحساب).
  - 💵 الخزنة (زرار على عنوان الصفحة).
  - الصفحات التلاتة دي ماكانتش أصلًا بتحمّل ملف `print-share.js` في
    الـ`<script>` بتاعها، فاتضاف تحميله ليها عشان الزرار يشتغل صح.
- لم يتغيّر أي منطق حسابات أو سلوك موجود — إضافة أيقونة/زرار فقط في
  كل نقطة من دول.

# v11-44 — 2026-09-05 — إصلاح تكرار الكروت في ملخصي العملاء/الأجهزة (دفعة تصحيحية)

⚠️ اتنفذ بمساعدة Claude، تحقق `node --check` بس من غير تشغيل فعلي في
متصفح. لازم يتجرب يدويًا قبل النشر.

- 🐛 دفعة v11-43 (تعميم شكل لوحة المخزن) ضافت صف كروت (`report-cards`)
  أعلى ملخصي العملاء والأجهزة، لكن نفس الأرقام دي كانت أصلاً معروضة
  كبلاطات (tiles) تحت في أقسام "حسب الحالة"/"متابعة" — فبقى فيه نفس
  الرقم ظاهر مرتين بشكلين مختلفين (كارت أفقي فوق، وبلاطة مربعة تحت).
  اتشال صف الكروت المكرر ده من الملخصين، وفضل بس الخط الرفيع الملوّن
  للتنبيه (متبقي غير محصل / متكرر الأعطال) وزرار الطباعة/المشاركة —
  دول مش مكررين وبيضيفوا قيمة فعلية. ملخص أوامر الشغل ما كانش فيه نفس
  المشكلة أصلاً (كان عنده كروت قابلة للضغط من الأول من غير إضافة صف
  تاني)، فاتسيب زي ما هو.
- لم يتغيّر أي منطق حسابات أو شكل بيانات في هذه الدفعة — عرض بس.

# v11-43 — 2026-09-05 — تكاملات بين الأقسام: عرض بيانات موجودة فعليًا في أماكن جديدة

⚠️ اتنفذ بمساعدة Claude، وتحقق `node --check` على كل ملف اتعدل +
محاكاة منطقية يدوية لدالة تنبؤ الاستهلاك بأرقام تجريبية، من غير تشغيل
فعلي في متصفح حقيقي. لازم يتجرب يدويًا قبل النشر: فتح ملف عميل عنده
أكتر من أمر شغل فيه قطع غيار والتأكد من ظهور "أكتر قطعة اتصرفت معاه"
وبادچ "🔁 يتكرر عطله" على الأجهزة المتكررة، فتح ملف جهاز عنده أكتر من
أمر والتأكد من جدول "قطع الغيار المستخدمة على هذا الجهاز"، فتح ملف
قطعة استُخدمت من قبل والتأكد من ظهور لينك أمر الشغل جنب كل حركة
ومن رقم "متوقع نفاذ الكمية"، وفتح ملخصات العملاء/الأجهزة/أوامر الشغل
والتأكد إن الكروت والخط الرفيع الملوّن وزرار الطباعة/المشاركة شغالين
زي المخزن بالظبط.

كل التعديلات دي **عرض بيانات موجودة بالفعل في localStorage في أماكن
جديدة** — مفيش أي تغيير في شكل البيانات نفسها ولا في أي حساب مالي.

- 🎨 **ملف العميل**: كارت جديد "🔧 أكتر قطعة اتصرفت معاه" (مجمّع من كل
  قطع الغيار في كل أوامره)، وبادچ "🔁 يتكرر عطله" على أي جهاز من
  أجهزته اتعمله أمرين أو أكتر داخل قائمة الأجهزة في نفس الصفحة (بدل ما
  تدخل كل جهاز لوحده في صفحة الأجهزة لتعرف).
- 🎨 **ملف الجهاز**: قسم جديد "🔧 قطع الغيار المستخدمة على هذا الجهاز
  (عبر كل الأوامر)" — جدول مجمّع (القطعة / الكمية الإجمالية / إجمالي
  المبلغ) بدل ما تدخل كل أمر شغل لوحده لتجمّع القطع يدويًا.
- 🎨 **ملف القطعة (`part.html`)**:
  - كل حركة في "آخر حركات المخزن" بقت بتعرض لينك مباشر لأمر الشغل
    المرتبط بيها (لو موجود) — البيانات (`requestId`) كانت متسجّلة من
    الأول جوه كل حركة خروج، بس مش معروضة هنا؛ نفس اللينك ده كان
    موجود بالفعل في صفحة "حركات الصنف" الكاملة (`part-moves.html`)
    من قبل.
  - كارت تنبؤ جديد "⏳ متوقع نفاذ الكمية خلال ... يوم": بيحسب معدل
    الاستهلاك اليومي من حركات "الخروج" في آخر 30 يوم (أو الفترة
    المتاحة لو الصنف أحدث من كده) ويقسم الكمية الحالية عليه. بيتلوّن
    تحذيري لو الأيام المتبقية 7 أيام أو أقل. ميظهرش لو مفيش حركات
    خروج كفاية لحساب معدل موثوق.
- 🎨 **تعميم شكل لوحة المخزن على ملخصات العملاء/الأجهزة/أوامر الشغل**:
  نفس نمط الكروت القابلة للضغط (`report-cards`) + الخط الرفيع الملوّن
  للتنبيه (`simple-line-bar`، أخضر لو مفيش تنبيه وأحمر لو فيه) + زرار
  طباعة/مشاركة (`psActions`) على عنوان الملخص، بقى مطبّق كمان على:
  - ملخص العملاء: كروت (إجمالي، عليه أمر مفتوح، جهاز في الورشة، متبقي
    غير محصل) + خط تنبيه للمتبقي غير المحصل.
  - ملخص الأجهزة: كروت (إجمالي، عليه أمر مفتوح، في الورشة، متكرر
    الأعطال) + خط تنبيه للأجهزة متكررة الأعطال.
  - ملخص أوامر الشغل: خط تنبيه للأوامر المتأخرة + زرار طباعة/مشاركة
    (كان الملخص من قبل بطاقات قابلة للضغط بالفعل، فاتضاف بس الجزء
    الناقص).
  - ملحوظة: زرار الطباعة/المشاركة (🖨️/↗️) بيستخدم `window.print()`
    (حفظ كـ PDF من نافذة الطباعة نفسها في أغلب المتصفحات) أو مشاركة/نسخ
    نص — مفيش أي مكتبة PDF منفصلة في المشروع، ومفيش تغيير في آلية
    الطباعة نفسها هنا، بس اتعمم استخدامها على الأقسام التلاتة دي.
- 🎨 **البحث الشامل (`global-search.js`)**: بقى بيغطي كمان:
  - 💳 **الحسابات والمحافظ**: حركات `K.wtx` (السبب/الملاحظة/التصنيف/
    اسم المحفظة)، ولينك النتيجة يودّي لصفحة تفاصيل المحفظة نفسها.
  - 🗺️ **خط السير**: أوامر الشغل اللي ليها موعد زيارة مجدول ولسه نشطة
    (مش مكتملة/مغلقة/ملغية) — بالبحث باسم العميل أو العنوان/المركز/
    القرية، ولينك النتيجة يودّي لملف أمر الشغل نفسه (مفيش رابط عميق
    لصفحة `route.html` بيوم أو مركز معيّن حاليًا).

# v11-42 — 2026-08-30 — دمج "مصاريف التشغيل": مصدر واحد بس (حركة محفظة فعلية) بدل نظامين منفصلين

⚠️ اتنفذ بمساعدة Claude، وتحقق منطقي كامل (`node --check` على كل ملف
اتعدل + محاكاة يدوية بـ Node لدالة الترحيل ودالة إضافة/حذف المصروف
والتقرير المالي وتأكيد النتايج) من غير تشغيل فعلي في متصفح حقيقي. لازم
يتجرب يدويًا قبل النشر: فتح التطبيق أول مرة بعد التحديث والتأكد إن
مصاريف كشف الحساب القديمة اتحولت تلقائيًا لحركات محفظة بنفس المبالغ
والتصنيفات (على أول محفظة في الإعدادات)، تسجيل مصروف تشغيل جديد من
صفحة الحسابات والتأكد إن رصيد المحفظة المختارة قل فعليًا، حذف مصروف من
نفس المكان والتأكد إنه اختفى من التقرير وبقى محذوف (soft-delete) من
كشف المحفظة، والتأكد إن أرقام "مصاريف التشغيل" في كشف الحساب وصفحة
"التقارير والتحليلات" وملخص الخزنة التشغيلي بقت كلها متطابقة ومصدرها
واحد.

- 🎨 **"مصاريف التشغيل" بقى نظام واحد بس بدل اتنين منفصلين**: كان فيه
  سجل مصاريف مستقل (تاريخ/تصنيف/مبلغ/ملاحظة) بيظهر في كشف الحساب بس
  ومالوش أي تأثير فعلي على رصيد أي حساب، وفي نفس الوقت حركة محفظة
  بتصنيف "مصروف تشغيل" بتقلل رصيد محفظة فعليًا. دلوقتي حركة المحفظة هي
  المصدر الوحيد: أي مصروف تشغيل جديد بيتسجل كحركة صرف فعلية من محفظة
  تختارها، ورصيدها بيتحدث تلقائيًا على طول.
- 🎨 **فورم "🧯 مصاريف التشغيل" في كشف الحساب** (جوه صفحة الحسابات) بقى
  بيطلب كمان "المحفظة" اللي هتتخصم منها، مع الحفاظ على التصنيف الفرعي
  (وقود ومواصلات / صيانة عدة وأدوات / إيجار وفواتير / أخرى...) والتاريخ
  والمبلغ والملاحظة زي ما كانوا، بالإضافة لخانة "السبب" الوصفية القصيرة.
  التصنيفات الفرعية دي لسه قابلة للإضافة والتعديل والحذف بالكامل من
  ⚙️ الإعدادات ← "تصنيفات مصاريف التشغيل (الفرعية)" 🧯 (بنفس آلية أي
  قائمة تانية في النظام).
- 🎨 **ترحيل تلقائي (migration) لأي مصروف قديم**: أول ما التطبيق يتحدّث،
  أي سجل من نظام المصاريف القديم بيتحول تلقائيًا لحركة محفظة بنفس
  التفاصيل بالظبط (نفس التاريخ والمبلغ والملاحظة، والتصنيف القديم بقى
  "تصنيف فرعي" على الحركة)، على أول محفظة موجودة في الإعدادات. السجل
  القديم بيفضل في مكانه كنسخة احتياطية بس من غير ما يقرأه أي كود تاني
  بعد كده — يعني مفيش أي بيانات بتتفقد.
- 🎨 **كل الأماكن اللي كانت بتحسب "مصاريف التشغيل" بقت بتسحب من نفس
  المصدر الموحّد**: كشف الحساب (شهري/أسبوعي)، صفحة "التقارير
  والتحليلات"، وملخص الخزنة التشغيلي (👁️ عرض فقط، من غير ما يتغير رصيد
  الخزنة نفسه). حذف مصروف من أي مكان (زرار 🗑️) بقى بيستخدم نفس آلية حذف
  حركة المحفظة (soft-delete مع تأكيد)، وبيختفي من كل الأماكن دي مع بعض.
- 🎨 صفحة الحساب التجميعي "🔧 حساب مصاريف الورشة (تشغيل)" (اللي بتتفتح
  من أيقونتها في صفحة الحسابات) بقت كمان بتعرض التصنيف الفرعي لكل حركة
  جنب تصنيفها الأساسي، وبتوضّح أي حركة اتنقلت (رُحّلت) من نظام كشف
  الحساب القديم.
- 🔧 "حذف كل البيانات التشغيلية" في الإعدادات بقى بيمسح حركات الحسابات
  (`K.wtx`) كمان (كانت ناقصة من القايمة قبل كده رغم إن ميزة الحسابات
  نفسها موجودة من زمان).

# v11-41 — 2026-08-30 — نقل "كشف الحساب" (التقرير المالي الشهري/الأسبوعي) من الواجهة الرئيسية لصفحة الحسابات + دمج عرض مصاريف التشغيل

⚠️ اتنفذ بمساعدة Claude وتحقق منطقي/`node --check` بس، من غير تشغيل فعلي في
متصفح. لازم يتجرب يدويًا قبل النشر: فتح الصفحة الرئيسية والتأكد إن قسم
"📈 كشف الحساب" مبقاش موجود فيها، فتح صفحة الحسابات (`wallets.html`)
والتأكد إن القسم بكل تفاصيله (شهري/أسبوعي، الجدول، مصاريف التشغيل،
تفاصيل كل بند) شغال زي ما كان بالظبط، والضغط على "💰 إيراد الشهر" من
الصفحة الرئيسية للتأكد إنه بيودي لصفحة الحسابات وينزل عند القسم صح.

- 🎨 **قسم "📈 كشف الحساب" (التقرير المالي الشهري/الأسبوعي) اتشال من
  الصفحة الرئيسية (`index.html`) ودخل جوه صفحة "الحسابات"
  (`wallets.html`)** زي ما هو بكل تفاصيله ووظائفه (التبديل شهري/أسبوعي،
  كل بنود الجدول والضغط عليها لعرض تفاصيلها، مصاريف التشغيل بالإضافة
  والحذف). رابط "💰 إيراد الشهر" في ملخص الواجهة الرئيسية بقى يودي
  لصفحة الحسابات مباشرة بدل تمرير داخل نفس الصفحة.
- 🎨 **دمج عرض مصاريف التشغيل في مكان واحد بدل التكرار**: كان فيه جدول
  "تفاصيل المصاريف" مجمّع حسب التصنيف تحت الجدول الرئيسي، وتحته منفصل
  قائمة "🧯 مصاريف التشغيل" الخام بكل حركة على حدة (فورم الإضافة + زرار
  الحذف). دلوقتي الاتنين مدموجين مكان واحد جوه نفس القسم: ملخص حسب
  التصنيف فوق، والحركات التفصيلية القابلة للحذف تحته — من غير أي فقد
  في الوظائف (الإضافة، الحذف، التجميع حسب التصنيف، تفاصيل كل بند من
  صف "مصاريف التشغيل" في الجدول الرئيسي).

# v11-40 — 2026-08-30 — تحويل مطوي + كشف الحساب جوه كل حساب + إعادة تسمية القسم لـ"الحسابات"

⚠️ اتنفذ بمساعدة Claude وتحقق منطقي/`node --check` بس، من غير تشغيل فعلي في
متصفح. لازم يتجرب يدويًا قبل النشر: فتح/قفل ودجت التحويل من زرار واحد في
صفحة الحسابات وصفحة الخزنة، تنفيذ تحويل والتأكد إن الودجت بيتقفل لوحده
بعد الحفظ، والتأكد إن الصفحة الرئيسية للحسابات بقت بدون كشف حركات طويل
وإن الكشف الكامل موجود جوه كل حساب لما تدوس على أيقونته.

- 🎨 **ودجت التحويل بقى زرار واحد قابل للضغط** (زي "⚡ أمر شغل سريع")
  بدل ما يكون ظاهر طول الوقت وياخد مساحة: دوس عليه يفتح نموذج التحويل،
  ودوس تاني يقفله. بعد تنفيذ التحويل بينقفل لوحده تلقائيًا.
- 🎨 **كشف الحركات الكامل بقى جوه كل حساب بس** بدل ما يتكرر في صفحة
  الحسابات الرئيسية كمان: الصفحة الرئيسية بقت أيقونات + رصيد إجمالي +
  ملخص تصنيفات مطوي بس، وأي تفصيل حركة كامل بيتشاف من جوه صفحة الحساب
  نفسه (`wallet.html`) لما تدوس على أيقونته.
- 🎨 **إعادة تسمية القسم من "المحافظ" إلى "الحسابات"** في كل الأماكن
  الظاهرة للمستخدم (كارت الرئيسية، عنوان الصفحة، صفحة التفاصيل، قسم
  الإعدادات)؛ أسماء الملفات الداخلية (`wallets.html`/`wallets.js`)
  وأسماء الحسابات الفردية (محفظتي الشخصية، فودافون كاش...) فضلت زي ما
  هي زي ما هي.
- 🧹 رُفع كاش Service Worker إلى v11-40.

# v11-39 — 2026-08-30 — أيقونات محافظ قابلة للضغط + تحويل بين المحافظ والخزنة

⚠️ اتنفذ بمساعدة Claude وتحقق منطقي/`node --check` بس، من غير تشغيل فعلي في
متصفح. لازم يتجرب يدويًا قبل النشر: الضغط على كل أيقونة في صفحة المحافظ
والتأكد إنها بتفتح صفحة التفاصيل الصح، إضافة/تعديل/حذف حركة من صفحة
التفاصيل، تنفيذ تحويل في الاتجاهين (محفظة←خزنة وخزنة←محفظة) من صفحة
المحافظ ومن صفحة الخزنة، والتأكد إن رصيد كل حساب بيتحسب صح بعد كده.

- ✨ **صفحة تفاصيل جديدة لكل حساب** (`wallet.html`): كل كارت في أول صفحة
  المحافظ (محفظتي الشخصية، فودافون كاش، أورنج كاش، إنستاباي، حساب
  مصاريفي الشخصية، حساب مصاريف الورشة...) بقى أيقونة قابلة للضغط بالاسم
  والرصيد، وبتفتح صفحة فيها كشف حركات كامل خاص بالحساب ده لوحده
  (`renderWalletDetail` في `wallets.js`)، مع إمكانية تسجيل حركة جديدة
  مباشرة لو كان الحساب محفظة فعلية.
- ✨ **تحويل بين المحافظ والخزنة**: ودجت جديد (`walletTransferWidgetHtml`)
  ظاهر في صفحة المحافظ وصفحة الخزنة، بيسمح بتحويل مبلغ من أي محفظة إلى
  درج الخزنة أو العكس. الحسابان يفضلوا مستقلين تمامًا في حساباتهم
  (زي ما كانوا بالظبط)؛ التحويل بيسجل حركة "صادر" في المصدر و"وارد" في
  الوجهة بس (`transferBetweenWalletAndTreasury`)، وبيتفلترهم `transferId`
  مشترك للمرجعية فقط من غير أي دمج تلقائي بين الحسابين.
- 🧹 `wallet.html` اتضافت لقائمة كاش Service Worker الأوفلاين
  (`CORE_FILES`)، ورُفع كاش Service Worker إلى v11-39.

# v11-37 — 2026-08-29 — قسم جديد: المحافظ (wallets.js + wallets.html)

⚠️ اتنفذ بمساعدة Claude وتحقق منطقي/`node --check` بس، من غير تشغيل فعلي في
متصفح. لازم يتجرب يدويًا قبل النشر: إضافة/تعديل/حذف محفظة وتصنيف من
الإعدادات، تسجيل عربون بمحفظة معينة وتعديل أمر الشغل والتأكد إن الحركة
اتحدّثت مش اتكررت، "تم الدفع بالكامل" باختيار محفظة، وزر الإدخال السريع
من الرئيسية، وتجربة الأوفلاين بعد أول تحميل.

- ✨ **قسم "المحافظ" الجديد** (منفصل تمامًا عن "الخزنة" اللي فضلت زي ما
  هي): كذا "مكان" فلوس فعلي (محفظة شخصية، محافظ موبايلات، إنستاباي...)
  ليه رصيد خاص بيه، وعلى عكس الخزنة، مربوط فعليًا بأوامر الشغل والمصاريف
  الشخصية/التشغيلية بدل ما يكون للعرض فقط.
- ✨ عربون أمر الشغل وتحصيل الإغلاق النهائي ("تم الدفع بالكامل") بقى
  فيهم اختيار اختياري لمحفظة الدفع؛ الاختيار ده بيسجل حركة "تحصيل عميل"
  تلقائيًا في `wallets.js` (`wf_wallet_tx`) مربوطة بنفس رقم أمر الشغل،
  وبتتحدّث (مش بتتكرر) لو الأمر اتعدّل تاني.
- ✨ حركات يدوية (وارد/صادر) بأي تصنيف من صفحة المحافظ، أو من زر
  "👛 حركة محفظة سريعة" الجديد بالرئيسية (نفس فكرة "⚡ أمر شغل سريع"
  بالظبط).
- ✨ أسماء المحافظ (`settings().wallets`) وتصنيفات حركة المحافظ
  (`settings().walletCategories`) بقيا قايمتين قابلتين للإضافة والتعديل
  والحذف والترتيب بالكامل من ⚙️ الإعدادات، بنفس آلية باقي القوايم
  (`listEditorHtml`/`addSettingItem`/...) من غير أي كود جديد مخصوص.
- 🧹 `wallets.html`/`wallets.js` اتضافوا لقائمة كاش Service Worker
  الأوفلاين (`CORE_FILES`) ولكل صفحات النظام اللي بتحمّل `treasury.js`،
  ورُفع كاش Service Worker إلى v11-37.

# v11-36 — 2026-08-28 — مراجعة عامة: تصحيح كاش أوفلاين + توحيد لغة الإشعارات

⚠️ اتنفذ بمساعدة Claude وتحقق منطقي/`node --check` بس، من غير تشغيل فعلي في
متصفح. لازم يتجرب يدويًا قبل النشر: تفعيل الإشعارات من الإعدادات والتأكد
إن النصوص بقت بالفصحى، وتجربة فتح التطبيق أوفلاين (وضع الطيران) بعد أول
تحميل والتأكد إن البحث الشامل شغال زي أي وضع أونلاين.

- 🐛 **`global-search.js` كان ناقص من قائمة تخزين Service Worker الأوفلاين**:
  محمّل في كل صفحة HTML لكن مش موجود في `CORE_FILES` بتاعت `service-worker.js`
  — يعني لو المستخدم فتح التطبيق كامل أوفلاين وكاش المتصفح العادي (HTTP
  cache) اتمسح لأي سبب، البحث الشامل كان ممكن يفشل يحمّل. اتضاف للقائمة.
- 🧹 **توحيد لغة رسائل الإشعارات للفصحى**: رسائل تفعيل الإشعارات
  (`enableNotifications`) ونصوص إشعارات "مواعيد اليوم"/"أوامر متأخرة"/"قطع
  منخفضة" كانت بالعامية المصرية بينما باقي التطبيق اتحول للفصحى قبل كده —
  اتوحدت في `app-notifications-bootstrap.js`.
- 🧹 **تبسيط `orderIsWorkshop()`**: كان فيها شرط إضافي زيادة عن
  `executionPlace === "الورشة"` بيتحقق من `workshopStatus`، لكن كل مكان
  بيغيّر `workshopStatus` بيحدّث `executionPlace` لـ"الورشة" في نفس اللحظة
  برضه، فالشرط الإضافي كان دايمًا بيرجع نفس النتيجة (كود ميت وظيفيًا، من
  غير أي تغيير في السلوك بعد التبسيط).
- ✅ مراجعة عامة مالقتش أي ملفات كود ميت راجعة، ولا أي دالة معرّفة مرتين من
  غير توثيق، ولا أي خطأ syntax في أي ملف JS.
- رفع كاش Service Worker إلى v11-36.

# v11-35 — 2026-08-28 — حذف التكرار من فلتر "التركيز" في أوامر الشغل

- فلتر "التركيز" كان فيه خيارات بترجع لنفس النتيجة بالظبط زي فلاتر تانية
  جنبها، فاتشالتوا:
  - "🟠 غير مكتمل" — كان مطابق حرفيًا لـ"🎯 المطلوب الآن" (نفس الشرط
    بالظبط)، فاتشال وفضل "المطلوب الآن" بس.
  - "🆕 جديد" و"🔧 جاري التنفيذ" و"❌ ملغي" — كانت مكررة مع خيارات فلتر
    "الحالة" المجاور ليها بالظبط، فاتشالوا من "التركيز"، وبقى اختيارهم
    من فلتر "الحالة".
  - "🏭 الورشة" — بقت مكررة بعد إضافة فلتر "📍 مكان التنفيذ" في v11-34،
    فاتشالت من "التركيز"، وبقى اختيارها من فلتر "مكان التنفيذ".
- اللي فضل في "التركيز" كله خيارات مالهاش بديل في فلتر تاني ومفيدة في
  البحث: كل الأوامر، المطلوب الآن، مكتمل (بيشمل المغلق كمان)، متأخر،
  انتظار قطع، اليوم، غير محصل.
- بطاقات الإحصائيات اللي فوق ("الجديد"، "جاري التنفيذ"، "الملغاة"،
  "الورشة") بقت بتوجّه لفلتر "الحالة"/"مكان التنفيذ" مباشرة بدل
  "التركيز"، عشان الفتح من البطاقة يفضل متزامن مع الفلاتر حتى لو
  استخدمت فلتر تاني بعد كده.
- رفع كاش Service Worker إلى v11-35.

# v11-34 — 2026-08-28 — تبسيط فلتر "الورشة" في قائمة أوامر الشغل

- فلتر "الورشة" في شاشة أوامر الشغل كان فيه 3 خيارات متداخلة وبتلخبط
  (🏭 في الورشة / 📦 مسحوب أو في الطريق / 🔧 داخل الورشة) وكلها بترجع لنفس
  فكرة "الجهاز في الورشة" من زوايا مختلفة، من غير أي خيار يوريك أوامر
  "المنزل" (عند العميل) لوحدها.
- بقى الفلتر خيارين بس وواضحين، مبنيين على مكان التنفيذ الفعلي للأمر:
  "🏭 في الورشة" و"🏠 في المنزل" (عند العميل) — واتغير اسم الفلتر لـ
  "📍 مكان التنفيذ" عشان يوصف اللي بيعمله فعلاً.
- تتبع حالة الجهاز داخل الورشة نفسه (تم السحب / تم التسليم) لسه موجود
  زي ما هو جوه صفحة الأمر 360° وكـ"مرحلة" — بس مش هيبقى ليه خيارات
  منفصلة في فلتر القائمة عشان منرجعش لنفس التداخل.
- رفع كاش Service Worker إلى v11-34.

# v11-33 — 2026-08-28 — دمج بطاقات الإحصائيات مع أزرار الفلترة في أوامر الشغل

- إزالة صندوق الأرقام الإحصائية المنفصل (باقي مفتوح، مرتبط بالورشة، متوسط الإكمال...) اللي كان بيكرر نفس بيانات أزرار الفلترة تحته.
- كل زرار فلترة بقى بيعرض رقمه (العدد) عليه مباشرة، والضغط عليه بيودّي لقائمة الأوامر المطابقة — بدل ما يكون الرقم في مكان والزرار في مكان تاني.
- دمج "باقي مفتوح" و"غير مكتمل" مع "المطلوب الآن" في زرار واحد بس (كانوا بيمثلوا نفس البيانات بالظبط).
- دمج "مرتبط بالورشة" مع زرار "الورشة"، وإضافة متوسط زمن تنفيذ الورشة كملاحظة صغيرة جوه نفس الزرار بدل بطاقة منفصلة.
- إضافة متوسط زمن الإكمال كملاحظة صغيرة جوه زرار "المكتملة" بدل بطاقة منفصلة.
- تقليل عدد العناصر في أعلى صفحة أوامر الشغل من 18 عنصر لـ 9 بس، لتوفير مساحة الصفحة.
- رفع كاش Service Worker إلى v11-33.

# v11-32 — 2026-08-28 — متابعة دورة أمر الشغل وتحليل زمن الإنجاز

- إضافة فلاتر تشغيلية لأوامر الشغل: المطلوب الآن، غير مكتمل، جديد، جاري التنفيذ، مكتمل، ملغي، متأخر، انتظار قطع، الورشة، اليوم، وغير محصل.
- إضافة ترتيب للأوامر حسب الأقدم، الأحدث، موعد الزيارة، عمر الأمر، وأطول مدة إكمال.
- إضافة مؤشرات سريعة لعدد الأوامر المفتوحة والجديدة والجارية والمتأخرة وانتظار القطع والورشة.
- تسجيل `startedAt` عند بدء التنفيذ و`completedAt` عند الوصول إلى مكتمل، مع الاحتفاظ بسجل الحالة السابق عند إعادة الفتح.
- حساب عمر الأمر المفتوح، مدة الإكمال من تسجيل البلاغ حتى مكتمل، ومدة تنفيذ أوامر الورشة.
- تسجيل `workshopEnteredAt` عند سحب الجهاز للورشة، وإظهار مدة بقاء الجهاز بالورشة عند توفر بياناتها.
- إضافة هذه الأزمنة إلى صفحة أمر الشغل 360°.
- لا توجد حالات جديدة ولا تغيير في دورة حالات أمر الشغل المعتمدة.
- رفع كاش Service Worker إلى v11-32.

# سجل التغييرات — الورشة الفنية (Workshop Mini)

هذا الملف يجمع كل ملاحظات التحديثات اللي كانت متفرقة في أكتر من ملف (README.md, README-V2.md, README.txt, README-BATCH2-CORRECTIONS.txt, README-V11.2.1-FIXES.txt, README-V11.2.6-SIMPLE-UI.txt, UPDATE-INSTRUCTIONS.txt, workshop-mini-enhancements.md) في مكان واحد مرتب بالتاريخ/رقم الإصدار (الأحدث فوق). الترقيم القديم كان غير منتظم (V2، Batch 2، V11.x بدون تسلسل ثابت)، فبعض الدفعات القديمة اتحطت تحت "دفعات مبكرة" لعدم وجود رقم إصدار واضح لها وقت كتابتها.

---

## v11-31 — 2026-08-27 — مهلة المرتجع بعد الإغلاق + ⚠️ نمط متكرر: ملفات محذوفة بترجع

⚠️ نفس ملحوظة الدفعات اللي قبلها: اتنفذ بمساعدة Claude وتحقق منطقي/`node --check`
بس، من غير تشغيل فعلي في متصفح. لازم تتجرب يدويًا قبل النشر: إغلاق أمر شغل
("تم الدفع بالكامل وإغلاق الأمر")، التأكد إن زرار "🔄 مرتجع" بيظهر جنبه وعليه
عدد الأيام المتبقية، تغيير "مهلة المرتجع" من الإعدادات وتجربة رقم صغير للتأكد
إن الزرار بيختفي فعلاً بعد انتهاء المهلة (تقدر تلاعب `closedAt` من أدوات
المطور لتسريع الاختبار)، والتأكد إن أمر الشغل بيرجع فعلاً قابل للتعديل بعد
الإرجاع.

- ✨ **مهلة "مرتجع" قابلة للتحديد لأوامر الشغل المكتملة والمغلقة**: قبل كده
  زرار "🔄 مرتجع" كان بيختفي خالص بمجرد إغلاق الأمر (`closed=true`). دلوقتي
  فيه إعداد جديد في صفحة الإعدادات ("🔄 مهلة المرتجع بعد إغلاق الأمر"، رقم
  أيام، افتراضيًا 7) — الأمر المغلق يفضل قابل للمرتجع/التعديل خلال المهلة دي
  من تاريخ `closedAt`، وبعدها الزرار بيختفي ويظهر بدل منه "⏹️ انتهت مهلة
  المرتجع". الأمر المكتمل وغير المغلق يفضل زي ما هو (بدون حد زمني). الدوال
  الجديدة `canReturnRequest`, `returnWindowDeadline`, `returnWindowDaysLeft`
  في `app-shared.js`. الإرجاع بيمسح `closed`/`paid` ويرجّع `remain` عشان
  التعديل يبقى متاح فعليًا (نفس شرط `canEdit` الموجود أصلاً).
- 🏗️ **توثيق التكرار المتعمد بين `app-requests.js`/`app-parts.js` وبين
  `workshop-mini-simple-ui.js`**: 4 دوال (`markPaidAndClose`, `closeOrder`,
  `renderParts`, `renderRequestFolders`) عندها نسخة أساسية في الملفات
  الأولى ونسخة أغنى بتستبدلها في الصفحات اللي بتحمّل `workshop-mini-
  simple-ui.js` — ده تحسين تدريجي مقصود مش خطأ، لكن كان موثّق في
  `renderRequestFolders` بس. اتضاف نفس التوثيق للتلاتة الباقيين، ومسح حقل
  زيادة (`r.closedStatus`) كان بيتسجل في نسخة `markPaidAndClose` بس مش في
  التانية (حقل مش مقروء في أي مكان، لكنه كان اختلاف فعلي بين النسختين).
- ⚠️ **ملحوظة مهمة — نفس ملفات الكود الميت رجعت للمرة التالتة**:
  `app-core.js`، `app.js`، `twms-ui.js`، `workshop-mini-classifications.js`،
  وكل ملفات الأيقونات/التوثيق المكررة اللي اتوثّق حذفها في v11-18، v11-21،
  وv11-30 — لقيتها كلها موجودة تاني في النسخة اللي اتبعتت في الجلسة دي،
  فاتحذفت تاني. الموضوع اتكرر أكتر من مرة رغم التوثيق في كل مرة، وده بيرجّح
  إن في حاجة في طريقة رفع/تحديث النسخة الحية بترجّع نسخة قديمة فوق الجديدة
  (Backup قديم بيتفك فوق الجديد، مجلد بيتنسخ من مصدر مش محدّث، أو مستودع Git
  فيه فرع/نسخة قديمة بترجع). يستاهل تتفقد قبل ما نكمل تنظيف تاني، وإلا هيرجع
  يحصل تاني.
- رفع رقم الكاش في Service Worker (`workshop-v11-31`).

---

⚠️ نفس ملحوظة الدفعات اللي قبلها: اتنفذ بمساعدة Claude وتحقق منطقي/`node --check`
بس، من غير تشغيل فعلي في متصفح. لازم تتجرب يدويًا قبل النشر: فتح صفحة الأوامر
والتأكد إن قسم "خط سير اليوم" بيعرض مواعيد اليوم بنفس شكل/أزرار صفحة خط
السير المستقلة (تسجيل الزيارة، غير متاح/لم يرد، ترتيب بالأسهم)، وإن تحريك
الترتيب أو تسجيل الزيارة من جوه صفحة الأوامر بينعكس صح لو فتحت صفحة خط
السير المستقلة بعدها والعكس، وتجربة زرار "🔄 مرتجع" على أمر شغل "مكتمل"
(من صفحة الأمر وصفحة قائمة الأوامر) والتأكد إنه بيطلب سبب/ملاحظة مطلوبة،
يرجّع الأمر لحالة "جاري التنفيذ" (نفس انتقال إعادة الفتح المعتمد في
WORK_ORDER_LIFECYCLE_APPROVED.md — مفيش حالة جديدة اتضافت)، وإن السبب
بيظهر في سجل تغييرات الحالة بالأمر.

- ✨ **خط سير اليوم داخل صفحة الأوامر**: قسم "خط السير القادم" المبسّط
  (روابط بس) في أول صفحة الأوامر اتستبدل بـ"خط سير اليوم" بنفس بطاقات
  وأزرار صفحة خط السير المستقلة (route.html) بالظبط — تجميع حسب المركز،
  إحصائيات اليوم، تسجيل/إلغاء الزيارة، حالة التواصل (غير متاح/لم يرد) مع
  إمكانية إعادة المحاولة، وترتيب الأوامر بالأسهم. الدوال المشتركة
  (`toggleVisited`, `setRouteContactStatus`, `moveRouteItem`, …) بقت
  بتحدّث أي عرض موجود فعليًا في الصفحة (route.html و/أو صفحة الأوامر) بدل
  ما تتقيّد بعنصر واحد بعينه، فالترتيب والحالة بيفضلوا متسقين بين الصفحتين.
- ✨ **زرار "مرتجع" جنب حالة "مكتمل"**: أمر الشغل المكتمل (وغير المقفول)
  بقى فيه زرار سريع "🔄 مرتجع" جنب حالته — في صفحة الأمر، قائمة الأوامر،
  وودجت خط سير اليوم. بيطلب سبب/ملاحظة المرتجع (مطلوبة زي سبب الإلغاء)
  ثم يرجّع الأمر لحالة "جاري التنفيذ" (نفس انتقال إعادة الفتح الموجود
  أصلاً في الدورة المعتمدة) عشان تقدر تفعّله أو تعدّل عليه، ويسجّل السبب
  في سجل تغييرات الحالة. لا حالة جديدة ولا تغيير في دورة الحالات المعتمدة
  (WORK_ORDER_LIFECYCLE_APPROVED.md).

---

## v11-29 — 2026-08-27 — تصحيحات: صور يتيمة، مخزون الإلغاء، الإيراد المحقق

⚠️ نفس ملحوظة الدفعات اللي قبلها: اتنفذ بمساعدة Claude وتحقق منطقي/`node --check`
بس، من غير تشغيل فعلي في متصفح. لازم تتجرب يدويًا قبل النشر: حذف جهاز/عميل
له صورة والتأكد إنها اتشالت (من حجم تخزين المتصفح أو أدوات المطور)، إلغاء
أمر شغل فيه قطع غيار من المخزن والتأكد إن الكمية رجعت، إعادة فتح أمر ملغي
والتأكد إن القطع اتخصمت تاني (أو ظهرت رسالة واضحة لو مش متاحة)، وفتح تقرير
الإيراد للتأكد إن قطع الغيار بقت بتظهر بس بعد قفل الأمر.

- 🐛 **حذف `app.js` (132KB) نهائيًا**: كان كود ميت 100% من مرحلة قبل تقسيم
  المشروع لملفات `app-*.js` — مش محمّل في أي صفحة HTML ولا في Service
  Worker، وفيه نسخة قديمة من نفس الدوال ممكن تلخبط أي تعديل مستقبلي لو
  اتفتح بالغلط بدل الملف الصحيح. نفس الحالة اللي اتصلحت قبل كده مع
  `app-core.js` و`twms-ui.js` في v11-18/v11-21.
- 🐛 **تنظيف صور الأجهزة اليتيمة في IndexedDB عند الحذف**: حذف جهاز مفرد
  (`deleteDeviceRecord`)، حذف عميل (بيسحب أجهزته)، `deleteAllDevices()`،
  `deleteAllCustomers()`، والمسح الشامل `deleteAllOperationalData()` كانوا
  بيمسحوا السجل من localStorage بس من غير ما يمسحوا صورة الجهاز المرتبطة
  من IndexedDB (`image-store.js`) — الصورة كانت تفضل محفوظة يتيمة للأبد
  وتاكل مساحة تخزين الجهاز بصمت. دلوقتي كل مسارات الحذف بتنضّف الصور
  المرتبطة، بنفس المنطق اللي كان مطبّق فعلاً على حذف قطعة الغيار
  (`deletePartRecord`) من الأول.
- 🐛 **إرجاع قطع الغيار للمخزن عند إلغاء أمر الشغل**: إلغاء أمر شغل (من
  التغيير السريع للحالة في القائمة، أو من صفحة التعديل الكاملة) كان بيغيّر
  الحالة لـ"ملغي" بس من غير ما يرجّع قطع الغيار المخصومة منه للمخزن —
  بعكس حذف الأمر نهائيًا اللي كان بيرجّعها صح. دلوقتي الإلغاء بيرجّع كل
  القطع المستخدمة، وإعادة فتح أمر ملغي (زي ما هو موثّق في
  `WORK_ORDER_LIFECYCLE_APPROVED.md`) بترجع تخصمها تاني لو لسه متاحة بنفس
  الكمية، وإلا بترفض إعادة الفتح برسالة واضحة بدل ما تفتح بمخزون غير متسق.
- 🏗️ **الإيراد المحقق: قطع الغيار بقت بتُحسب للأوامر المقفولة نهائيًا بس**،
  بنفس منطق المصنعية تمامًا. قبل كده كانت المصنعية بتتحسب من الأوامر
  المقفولة بس، لكن قيمة قطع الغيار وتكلفتها كانت بتتحسب من كل أوامر الفترة
  بغض النظر عن حالة الإغلاق — يعني أمر لسه "جاري التنفيذ" كانت قطعه بتدخل
  في "الإيراد" و"المكسب" وهو لسه ماتقفلش. اتصلح في `financeReport()`
  و`reportRowMeta()` (`app-dashboard-reports.js`) وتقرير الطباعة الكامل
  (`reports.js`) مع بعض عشان يفضلوا متطابقين.
- رفع رقم الكاش في Service Worker (`workshop-v11-29-fixes`) عشان الأجهزة
  اللي عندها نسخة مثبتة تاخد كل التصحيحات دي.

---

## v11-30 — 2026-08-27 — استكمال المراجعة: Restore، إيراد الشاشة الرئيسية، توحيد التاريخ، تنظيف التوثيق

⚠️ نفس ملحوظة v11-29: فحص كودي/منطقي + `node --check` بس. لازم تُختبر يدويًا:
استرجاع نسخة احتياطية قديمة على جهاز فيه صور مختلفة والتأكد إن الصور
القديمة اتشالت وبقيت صور الملف بس، ومراجعة رقم "إيراد الشهر" في الشاشة
الرئيسية مقابل تقرير الشهر نفسه للتأكد إنهم متطابقين.

- 🐛 **تنظيف IndexedDB قبل استرجاع نسخة احتياطية**: `restoreBackupFile`
  كان بيستورد صور الملف بـ `ImageStore.importAll` من غير ما يمسح صور
  الجهاز الحالية الأول — يعني صور من قبل الاسترجاع (بمراجع مختلفة عن
  اللي في الملف) كانت بتفضل موجودة يتيمة جنب صور النسخة المستوردة، رغم
  إن رسالة التأكيد بتقول إن الاسترجاع "هيستبدل كل بيانات النظام الحالية".
  دلوقتي بيتعمل `ImageStore.clearAll()` قبل الاستيراد مباشرة.
- 🐛 **نفس ثغرة "الإيراد المحقق" كانت موجودة في الشاشة الرئيسية**: بند
  "💰 إيراد الشهر" في `renderDash()` كان بيحسب قطع الغيار من كل أوامر
  الشهر بغض النظر عن الإغلاق (نفس الثغرة اللي اتصلحت في صفحة التقارير
  في v11-29، لكنها فاتت وقتها من `renderDash` تحديدًا). دلوقتي بيتحسب من
  الأوامر المقفولة بس، ومطابق تمامًا لرقم صفحة التقارير لنفس الشهر.
- 🏗️ **توحيد 3 نسخ مكررة من نفس منطق "تاريخ اليوم المحلي"**:
  `localDateKey` (`app-shared.js`)، `dayKeyLocal` (`app-shared.js` برضه)،
  ودالة محلية باسم `dateKey` جوه `reports.js` — التلاتة بيعملوا نفس
  الحساب بالظبط. بقى `dayKeyLocal` هو المصدر الوحيد (الأكثر أمانًا لأنه
  بيتعامل مع أي مدخل ومعاه فحص NaN)، و`localDateKey` و`dateKey` بقوا
  مجرد أسماء بديلة (alias) ليه بدون أي تغيير في أي استدعاء حالي.
- 🐛 **حذف ملفات README/توثيق قديمة** كان محتواها مدموج بالفعل في
  `CHANGELOG.md` من دفعة سابقة لكن الملفات الفعلية فضلت موجودة:
  `README-BATCH2-CORRECTIONS.txt`، `README-V11.2.1-FIXES.txt`،
  `README-V11.2.6-SIMPLE-UI.txt`، `README-V2.md`، `README.txt`،
  `UPDATE-INSTRUCTIONS.txt`، `workshop-mini-enhancements.md`. اتأكد قبل
  الحذف إن مفيش أي كود بيشاور عليهم. `README.md` (الحالي) و
  `WORK_ORDER_LIFECYCLE_APPROVED.md` (مرجع معتمد يُستخدم فعليًا في
  `migrations.js`) فضلوا زي ما هم.
- رفع رقم الكاش في Service Worker (`workshop-v11-30-fixes2`).

---

## v11-22 — 2026-08-26 — فصل app.js تدريجيًا: الخزنة والمهام في ملفات مستقلة

⚠️ نفس ملحوظة v11-21: اتنفذ بمساعدة Claude وتحقق منطقي/`node --check` بس،
من غير تشغيل فعلي في متصفح. لازم تتجرب يدويًا قبل النشر: فتح صفحة الخزنة
(إضافة حركة وارد/صرف، حفظ رصيد افتتاحي)، وصفحة المهام (إضافة/تعديل/حذف
مهمة)، وحفظ أمر شغل بعربون (بينادي `syncTreasuryForOrderDeposit`) للتأكد
إن التنقّل بين الملفات مأثرش على السلوك.

- 🏗️ **قسم الخزنة اتنقل بالكامل من `app.js` لملف `treasury.js` جديد** (13 دالة: `treasuryEntries`, `treasuryBalance`, `upsertTreasuryEntry`, `removeTreasuryEntry`, `syncTreasuryForOrderDeposit`, `syncTreasuryForOrderClose`, `syncTreasuryForExpense`, `addTreasuryManual`, `saveOpeningBalance`, `editTreasuryEntry`, `deleteTreasuryEntry`, `operationalTreasurySummary`, `renderTreasury`) — نفس الكود حرفيًا، نقل مكان بس.
- 🏗️ **قسم المهام والمتابعة اتنقل بالكامل لملف `tasks.js` جديد** (`taskRows`, `taskDateTime`, `taskPriorityClass`, `saveTask`, `clearTaskForm`, `toggleTask`, `deleteTask`, `editTask`, `renderTasks`, `initTasks`, `fillTaskRequests`) — نفس الكود حرفيًا برضه.
- 📉 **`app.js` نزل من 810 لـ 638 سطر** في الدفعتين v11-21 وv11-22 مع بعض (كانت أكبر قبل كده).
- الصفحات الخمستاشر و`service-worker.js` اتحدثوا يحمّلوا `treasury.js` و`tasks.js` قبل `app.js` (اسم الـ cache اتغيّر تاني عشان الإصدار الجديد ينزل).
- لم يتغيّر أي منطق أو سلوك ظاهر — نقل كود بس، مفيش أي تعديل في محتوى الدوال.

## v11-21 — 2026-08-26 — نسخة بيانات (schema version) + نقل الصور لـ IndexedDB + طبقة rollback + تنظيف ملفات ميتة (باقية من v11-18)

⚠️ **الدفعة دي اتنفذت بمساعدة Claude من غير تشغيل فعلي في متصفح حقيقي** (فحص
كودي/منطقي + `node --check` للتأكد من عدم وجود أخطاء syntax بس). لازم تتجرب
يدويًا قبل ما تتنشر: إضافة/تعديل جهاز وقطعة بصورة، فتح ملف الجهاز/القطعة
360° للتأكد إن الصورة بتظهر، تصدير نسخة احتياطية واسترجاعها، وفتح صفحة فيها
بيانات قديمة (قبل التحديث) للتأكد إن الترحيل شغال صح.

- 🐛 **تنظيف فعلي أخيرًا لملفات كان اتوثّق حذفها في v11-18 لكنها فضلت موجودة**: `app-core.js` (كان غير محمّل في أي صفحة أصلًا — كود ميت 100%)، `workshop-mini-classifications.js`، `twms-ui.js`، وصور الأيقونات المكررة القديمة (`icon-192.png`, `icon-192-v2.png`, `icon-192-v11-2-7.png`, `icon-512.png`, `icon-512-v2.png`, `icon-512-v11-2-7.png`). اتأكد قبل الحذف إنها مش متسجلة في أي `<script>`/`<link>` ولا في `service-worker.js` ولا `manifest.json`.
- 🏗️ **`wf_schema_version` في `shared-data.js`**: رقم إصدار لشكل البيانات المحفوظة، منفصل عن رقم إصدار التطبيق. بيانات قديمة من غير الرقم ده تتعتبر إصدار 1 ضمنيًا.
- 🏗️ **ملف جديد `image-store.js`**: تخزين الصور في IndexedDB (`workshopImagesDB`) بدل base64 جوه سجل الجهاز/القطعة في localStorage. سجل الجهاز/القطعة بقى بيحمل مرجع (id) بس. توافق كامل مع البيانات القديمة: أي قيمة `photo` لسه بادئة بـ`data:` بترجع زي ما هي من غير أي كسر.
- 🏗️ **ملف جديد `migrations.js`**: بيشغّل ترحيل 1→2 مرة واحدة بس (نقل أي صورة base64 قديمة في `wf_d`/`wf_p` لـ IndexedDB واستبدالها بمرجع)، بينادَى ويتنتظر (`await`) في أول حاجة في `DOMContentLoaded` قبل أي رندر.
- 🏗️ **`withRollback(keys, fn)` في `shared-data.js`**: تعميم نمط الـ backup/restore اليدوي اللي كان موجود في `persistRequestRecord` (باستخدام `JSON.stringify` وإرجاعه لو فشل) في مكان واحد، وتطبيقه على `wf_p` و`wf_m` مع بعض بدل `wf_p` بس.
- 🏗️ **الباك أب/الاسترجاع (`backupAllData`/`restoreBackupFile`) بقوا بيشملوا الصور** (من IndexedDB) ورقم `schemaVersion`، مع فضل الملف الناتج JSON واحد قائم بذاته زي ما كان قبل كده.
- 🏗️ صفحات HTML الخمستاشر اللي بتحمّل `app.js` بقت بتحمّل `image-store.js` و`migrations.js` قبله، و`service-worker.js` بقى بيعمل precache لهم (اتغيّر اسم الـ cache عشان يتنزل الإصدار الجديد).
- لم يتغيّر أي شكل بيانات ظاهر للمستخدم ولا سلوك واجهة في أي شاشة تانية غير المذكور فوق.

## v11-18 — 2026-08-26 — تنظيف الكود الميت (تنفيذ فعلي) + توحيد طبقة البيانات

هذه الدفعة نفّذت فعليًا التنظيف الموثّق في الدفعة اللي قبلها (كان اتكتب إنه تم لكن الملفات فضلت موجودة فعليًا في آخر نسخة مرفوعة)، بالإضافة لخطوة معمارية جديدة:

- ✅ حذف فعلي لـ `app-core.js`، `workshop-mini-classifications.js`، `twms-ui.js` (كانت لسه موجودة رغم توثيق حذفها) وصور الأيقونات المكررة (`icon-192.png`, `icon-192-v2.png`, `icon-192-v11-2-7.png`, `icon-512.png`, `icon-512-v2.png`, `icon-512-v11-2-7.png`). الحجم رجع من ~2.1MB لـ ~660KB.
- 🐛 حذف قائمة "🗺️ كل المراكز" (`customerCenterFilter` / `deviceCenterFilter`) من `customers.html` و`devices.html` — كانت موجودة بصريًا لكن بدون أي تأثير فعلي، لأن نسخة العرض النهائية (`workshop-mini-simple-ui.js`) بتستبدل دوال العرض بالكامل ومبتقراش الفلتر ده. كانت بتظهر للمستخدم إنها شغالة وهي مش شغالة.
- 🏗️ **ملف جديد `shared-data.js`**: مصدر واحد لكل الدوال المشتركة (`K`, `get`, `put`, `arr`, `esc`, `id`, `settings`, `duplicateCustomerByPhone`, `customerName`, `deviceName`, `addresses`, `addressText`) بدل ما تكون معرّفة بنفس المنطق في 3 ملفات مختلفة (`app.js`, `workshop-mini-enhancements.js`, `workshop-mini-simple-ui.js`). لازم يتحمّل قبل أي ملف تاني — مضاف لكل صفحات HTML وموجود في قائمة تخزين الـ Service Worker.
- 🏗️ **`window.defineOverride(name, sourceFile, fn)`**: أداة بسيطة بتوثّق أي مرة دالة `render*` بتتبدّل من ملف تاني (نفس السلوك بالظبط، بس بقى موثّق في الكونسول بدل ما يحصل بصمت). اتطبّقت على `renderCustomers`, `renderDevices`, `renderRequests`, `customerProfile`, `deleteCustomerRecord`, `deleteDeviceRecord`.
- لم يتغيّر أي منطق حسابات أو شكل بيانات أو سلوك ظاهر للمستخدم في هذه الدفعة.

## v11-19 — 2026-08-26 — فصل منطق الحفظ عن الـ DOM + توحيد تحديث الشاشات

- 🏗️ **`saveRequest()` (حفظ أمر الشغل) اتقسمت لـ 3 دوال**: `collectRequestFormData()` (قراءة قيم الفورم فقط)، `persistRequestRecord()` (منطق الحفظ والمخزون الصِرف — بدون أي لمسة DOM، وبيرجع `{ok, request}` أو `{ok:false, error}`)، و`saveRequest()` كدالة تحكم رفيعة بتربط بينهم. نفس ترتيب العمليات، نفس الحسابات، نفس رسائل الخطأ بالظبط — اتأكدت بمحاكاة تشغيل فعلية (أمر جديد، تعديل بكمية مخزون غير متاحة مع التراجع، تعديل ناجح بيقلل الكمية المستخدمة) وطابقت النتائج المتوقعة تمامًا.
- 🏗️ **`refreshAllScreens()` في `shared-data.js`**: نقطة واحدة لتحديث كل الشاشات (`renderCustomers`, `renderDevices`, `renderRequests`, `renderDash`, `monthReport`) بعد حذف عميل أو جهاز، بدل ما تتكرر يدويًا (بترتيب مختلف كل مرة) في أكتر من مكان في `workshop-mini-enhancements.js`.
- لم يتغيّر أي منطق حسابات أو شكل بيانات أو سلوك ظاهر للمستخدم في هذه الدفعة.

## v11-20 — 2026-08-26 — نفس فصل الحفظ عن الـ DOM لباقي النماذج

استكمال لنفس النمط اللي اتطبّق على أمر الشغل في الدفعة اللي قبلها، اتطبّق على باقي شاشات الحفظ:

- 🏗️ **حفظ العميل**: `collectCustomerFormData()` (قراءة الاسم/الهاتف/العنوانين) + `persistCustomerRecord()` (حفظ صِرف). تأكيد "رقم الهاتف مكرر" (`confirm()`) فضل في المعالج نفسه لأنه تفاعل مباشر مع المستخدم مش منطق بيانات.
- 🏗️ **حفظ الجهاز**: `collectDeviceFormData()` (بما فيها تحويل الصورة) + `persistDeviceRecord()` (بترجع `{ok, device}`).
- 🏗️ **حفظ القطعة**: `collectPartFormData()` + `persistPartRecord()` بنفس الشكل.
- كل الدوال الجديدة اتفحصت بمحاكاة تشغيل فعلية: إنشاء عميل/جهاز/قطعة، تعديل سجل موجود، وحالة امتلاء مساحة التخزين (تفشل بهدوء بنفس رسالة التنبيه القديمة بالظبط من غير ما تنتقل الصفحة). النتائج طابقت السلوك الأصلي تمامًا.
- لم يتغيّر أي منطق حسابات أو شكل بيانات أو سلوك ظاهر للمستخدم في هذه الدفعة.


## أحدث تعديلات — 2026-08-26

**تحسين شكل "خط السير القادم" + إعدادات المظهر**
- إعادة تصميم بطاقة "📅 خط السير القادم" في الشاشة الرئيسية بشكل بطاقة مستقلة (بدل الأسلوب القديم).
- إصلاح تعارض كان موجود بين class باسم `route-summary`/`route-group-title` مستخدم في حاجتين مختلفتين (صفحة `route.html` القديمة، وودجت الشاشة الرئيسية) — بقى لكل واحدة أسماء classes مستقلة (`rp-*`).
- إضافة إعداد جديد في صفحة الإعدادات: مظهر خط السير القادم (تلقائي حسب النظام / داكن / فاتح / لون مخصص).

**إصلاحات تشغيل يومي (Service Worker / PWA)**
- إصلاح مرجعين لأيقونة إشعارات قديمة (`icon-192-v11-2-7.png`) كانوا لسه موجودين في `service-worker.js` و`app.js` بعد ما باقي المشروع اتحدّث لأيقونة أحدث — بقوا كلهم يشاورو على نفس الأيقونة الحالية.
- إضافة `workshop-mini-simple-ui.js` و`workshop-mini-enhancements.js` لقائمة الملفات المحفوظة أوفلاين (Service Worker) — كانوا ناقصين مع إنهم أساسيين لصفحات أوامر الشغل/العملاء/الأجهزة.
- رفع رقم نسخة الكاش عشان الأجهزة اللي عندها نسخة مثبتة تاخد التحديثات دي.

**تنظيف الكود**
- حذف ملفات كانت غير مستخدمة خالص في أي صفحة: `app-core.js` (نسخة Core بديلة قديمة)، `workshop-mini-classifications.js`، `twms-ui.js`.
- حذف نسخ أيقونات مكررة بايت-لبايت (كان فيه 3 نسخ متطابقة من نفس أيقونة 192px، و3 نسخ من أيقونة 512px) وإبقاء أحدث نسخة بس من كل مقاس.
- حجم المشروع نزل من ~2.1MB إلى ~644KB.
- دمج كل ملفات التوثيق المتفرقة في هذا الملف.

---

## V11.11
- قطعة غيار خارج المخزن: تضاف داخل أمر الشغل (اسم + سعر شراء + سعر بيع) ومالهاش أي علاقة بالمخزن أو كمياته أو أرقام قطع الغيار في التقارير. مكسبها فقط (سعر البيع − سعر الشراء) بيتضاف تلقائيًا لبند "المصنعية" وفي كل حسابات الإيراد والمكسب.
- تنسيق كامل لصفحة التقارير والتحليلات (بطاقات، جداول، قوائم) — كانت من غير أي CSS، بقت متناسقة ومتجاوبة على الموبايل ومهيأة للطباعة.
- تنسيق الأرقام المالية في التقارير بفواصل الآلاف لسهولة القراءة.
- رفع رقم الكاش في Service Worker.

## V11.10 — واجهة الطباعة والمشاركة
- إزالة أزرار الطباعة والمشاركة العامة من الصفحات.
- إضافة أزرار صغيرة داخل سياق العميل والجهاز وأمر الشغل وقطعة المخزن فقط.
- إضافة مهام اليوم إلى إحصائيات الصفحة الرئيسية بدل بطاقة مستقلة.
- تقرير الورشة يعتمد على الحالة الحالية للأجهزة/الأوامر داخل الورشة.

## PWA ICON UPDATE V11.2.7
- رفع رقم نسخة الـ manifest والـ Service Worker cache.
- استخدام أسماء ملفات أيقونة جديدة فريدة عشان أندرويد ميحتفظش بالأيقونة القديمة القديمة في الكاش.
- بيانات/تخزين التطبيق ماتأثرتش بتحديث الأيقونة ده.

## V11.2.6 — Simple UI
- تبسيط واجهة أوامر الشغل وإزالة تكرار بطاقات الأوامر.
- ملخص واحد لأوامر اليوم، الورشة، المكتملة، انتظار قطع الغيار، والمتأخر.
- زر «كل الأوامر» لعرض القائمة التفصيلية عند الطلب فقط.
- العملاء والأجهزة والمخزن يبدأون بملخص مختصر، والقائمة الكاملة تظهر بزر «كل ...».
- خط السير القادم يجمع الأوامر حسب التاريخ ثم المركز ثم القرية.
- المراكز والقرى تعتمد على الإعدادات الحالية، ويمكن إضافة قرى لاحقًا من غير تعديل كود.
- عند إغلاق أمر الشغل يتم تسجيله كمغلق مع تثبيت الحالة «مكتمل» وتاريخ الإغلاق.
- لا يتم تغيير بنية localStorage أو حذف البيانات الحالية.

### BATCH 2.6 HOTFIX — 2026-08-21
- توحيد استرجاع أوامر الشغل من المفاتيح القديمة/الأساسية من غير استبدال البيانات الموجودة.
- إضافة زر إغلاق مباشر للأمر المكتمل من قائمة أوامر الشغل.
- تحسين إغلاق الأمر وتحديث القائمة مباشرة.
- إصلاح توقف صفحة أوامر الشغل عن عرض القائمة بسبب اعتماد على عناصر DOM غير معرفة صراحة (`requestSearch`/`statusFilter`/`workshopFilter`) — اتربطت مباشرة عبر `getElementById`.
- تحديث نسخة Service Worker.

## V11.2.3
- تحديث ملفات ثابتة أونلاين أولًا (online-first) مع fallback أوفلاين ونسخة كاش جديدة.

## V11.2.1 Patch
- إصلاح فتح ملفات 360° أوفلاين (`customer.html?id=`, `device.html?id=`, `request.html?id=`, `part.html?id=`) بالتعامل مع الصفحة حسب المسار من غير احتساب query string، مع بقاء الـ ID في الرابط ليقرأه `app.js`.
- إصلاح أماكن أزرار "فتح 360°" و"حذف" في كروت العملاء والأجهزة وأوامر الشغل — بقت في عمود مستقل أقصى اليسار من غير تداخل مع البيانات.
- رفع رقم Cache للـ Service Worker.
- فحص syntax لملفات JavaScript وHTML بنجاح.

## V11.2
- تحديث أيقونات PWA/التطبيق باستخدام لوجو الورشة؛ منطق التطبيق لم يتغير.

## V11.1
- Manifest للتثبيت كتطبيق على الهاتف.
- Service Worker للتشغيل Offline بعد أول تحميل ناجح.
- Cache لملفات النظام الأساسية.
- تحديث ملفات الواجهة عند توفر الإنترنت من غير لمس بيانات localStorage.
- زر تثبيت التطبيق يظهر عندما يسمح المتصفح بذلك.

---

## دفعات مبكرة (بدون رقم إصدار ثابت وقتها)

### تحديث المهام والخزنة (الدفعة 01)
الملفات المتأثرة وقتها: `app.js`, `index.html`, `treasury.html`, `service-worker.js`, `tasks.html`.
- إضافة قسم المهام والمتابعة.
- فصل الخزنة عن تحصيلات أوامر الشغل ومصاريف التشغيل.
- الخزنة بقت درج نقدي مستقل: رصيد افتتاحي + وارد + صرف + سبب + لمن/من + المكان + تفاصيل.
- عرض ملخص حسابات التشغيل داخل الخزنة للمتابعة فقط، من غير إدخالها في رصيد الدرج.
- تحديث Service Worker ليكاش صفحة المهام.

### Workshop Mini V2 — Focused Enhancements
نطاق التعديل: العملاء والأجهزة فقط، بدون أقسام جديدة.
- تحسين بحث العملاء (الاسم، الهاتف، العنوانين).
- ملف العميل 360°: ملخص أوضح + الأجهزة + أوامر الشغل المرتبطة.
- حذف العميل بقى متاح مع تحذير وتأكيد واضح، وإعادة قطع الغيار للمخزن.
- حذف الجهاز بقى متاح مع تحذير وتأكيد واضح، وإعادة قطع الغيار للمخزن.
- تحسين بحث الأجهزة (العميل، النوع، التصنيف، الماركة، الموديل، الوصف).
- إصلاح helper مفقود خاص باكتشاف رقم هاتف عميل مكرر، مع إبقاء قرار إنشاء العميل للمستخدم.
- لا توجد إضافات تخص الفنيين أو Backend أو الولاء أو الإشعارات أو أي قسم جديد.

> ملحوظة: كانت هذه الدفعة مبنية على مواصفة تفصيلية طويلة (`workshop-mini-enhancements.md`) بخطوط عريضة زي: الحفاظ على 4 أقسام بس (عملاء/أجهزة/أوامر شغل/مخزن)، تحسين الترابط بينهم وصحة الحسابات وقطع الغيار، تحسين رسائل النجاح/الخطأ والتأكيد قبل الحذف، وعدم إضافة فنيين/بوابة عميل/ولاء/Backend في هذه المرحلة. أغلب بنودها اتنفذت وبقت جزء من النظام الحالي، فتم الاكتفاء بالملخص ده بدل نقل المواصفة كاملة.

### Batch 2 corrections
- الكاميرا بقت تلتقط صورة واحدة في كل ضغطة وتتجمع الصور من كذا ضغطة كاميرا.
- استبدال حقول اختيار العميل/الجهاز/العنوان المكررة الشكل بمُنتقي واحد قابل للبحث لكل واحد.
- بنية البيانات ومفاتيح localStorage لم تتغير.
- ملحوظة: النسخة لسه بتخزن بيانات الصور في تخزين المتصفح؛ تخزين دائم على سيرفر يحتاج Backend/Object Storage منفصل، ومش متوفر في هذه المرحلة.

### إضافة مجلدات أوامر الشغل (تجربة قديمة — تم تجاوزها)
كانت هذه الدفعة بتضيف تصنيف أوامر الشغل في 4 مجلدات (مكتملة/الورشة/اليوم/انتظار قطع غيار)، عن طريق استبدال `requests.html` وملف Core منفصل باسم `app-core.js`.
> ملحوظة: `app-core.js` كان نظام Core بديل بمفاتيح تخزين مختلفة تمامًا عن اللي شغال دلوقتي، ولم يكن مستخدمًا فعليًا في أي صفحة HTML بالمشروع الحالي. تم حذفه بتاريخ 2026-08-26 ضمن تنظيف الكود الميت (نفس فكرة "المجلدات" اتحققت لاحقًا بشكل مختلف عبر ملخصات أوامر الشغل في V11.2.6).
