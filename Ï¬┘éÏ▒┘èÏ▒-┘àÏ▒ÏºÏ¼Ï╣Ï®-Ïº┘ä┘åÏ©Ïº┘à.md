# تقرير مراجعة نظام الورشة الفنية

## الخلاصة التنفيذية

نعم، النظام يحتاج إلى تحسينات، لكن الحالة الحالية **مقبولة كتطبيق PWA محلي لمستخدم واحد** وليست حالة انهيار أو وجود أخطاء نحوية واضحة. تمت مراجعة ملفات الإصدار المرفق، وعددها تطبيق ويب ثابت يعتمد على HTML وJavaScript وCSS، مع تخزين البيانات في `localStorage` والصور/التسجيلات في `IndexedDB`.

أهم استنتاج: **المشكلة الأكبر ليست في قسم أكواد الكباسات نفسه، بل في طبقة البيانات والمعاملات والتوسع المستقبلي**. إذا كان الاستخدام على جهاز واحد ونسخ احتياطي يدوي، يمكن الاستمرار بعد إصلاحات متوسطة الأولوية. أما إذا كان المطلوب استخدامًا إنتاجيًا فعليًا من أكثر من جهاز أو أكثر من فني، فلابد من Backend وقاعدة بيانات وصلاحيات ومزامنة.

## ما تم التحقق منه

| الفحص | النتيجة |
|---|---|
| فحص صياغة جميع ملفات JavaScript بواسطة `node --check` | نجح لجميع الملفات |
| الروابط المحلية المشار إليها من صفحات HTML وملف الخدمة | لا توجد ملفات مفقودة |
| قائمة ملفات Service Worker | متوافقة مع ملفات التطبيق الموجودة |
| قاعدة أكواد الكباسات | 56 ماركة و12,202 سجل، وكل السجلات المفحوصة لها بنية صحيحة وموديل |
| وجود اختبارات آلية | لا توجد حزمة اختبارات أو ESLint/Prettier/Playwright مرفقة |
| طبيعة التخزين | محلي فقط؛ لا مزامنة ولا مستخدمون ولا صلاحيات حقيقية |

## الملاحظات حسب الأولوية

### أولوية عالية

#### 1. الحفظ متعدد الخطوات ليس ذريًا في كل المسارات

بعض العمليات تعدّل أكثر من مفتاح تخزين متتابعًا، مثل إضافة قطعة إلى أمر شغل أو حذف أمر شغل أو استرجاع نسخة احتياطية. الدالة `put()` تمسك خطأ امتلاء التخزين وتعيد `false`، لكن عدة مستهلكين لا يفحصون قيمة الإرجاع. لذلك يمكن نظريًا أن ينجح تحديث المخزون، ثم يفشل حفظ حركة المخزن أو الأمر، فتظهر البيانات في حالة غير متسقة.

يوجد بالفعل `withRollback` في `shared-data.js`، وهذا اتجاه جيد، لكنه غير مطبق على كل العمليات الحساسة. كذلك الاستيراد الاحتياطي يكتب المفاتيح واحدًا تلو الآخر ولا يملك مرحلة تحقق كاملة أو تراجعًا عند فشل خطوة لاحقة.

**التحسين المقترح:** توحيد كل العمليات متعددة السجلات داخل Transaction Service واحدة، أو نقل البيانات إلى IndexedDB باستخدام معاملات حقيقية. عند الحد الأدنى، يجب أن تفحص كل عملية نتيجة `put()`، وأن تحتفظ بلقطة لكل المفاتيح وتستعيدها عند أي فشل.

#### 2. الحذف العام قد يترك النظام في حالة جزئية

وظائف حذف جميع العملاء أو الأجهزة أو الأوامر تعدّل المخزون، حركات المخزن، المحافظ، الصور، التسجيلات، والعملاء/الأجهزة/الأوامر في عدة عمليات منفصلة. إذا انقطع التطبيق أو امتلأت المساحة أثناء التنفيذ، لا يوجد ضمان لاستكمال العملية أو إرجاعها.

**التحسين المقترح:** تنفيذ الحذف العام كعملية مرحلية قابلة للتسجيل، مع رسالة تقدم، والتحقق من نجاح كل كتابة، ثم تنظيف IndexedDB في خطوة نهائية. الأفضل استخدام IndexedDB لكل البيانات المترابطة بدل توزيع الحالة بين `localStorage` و`IndexedDB`.

#### 3. الاسترجاع من النسخة الاحتياطية يحتاج تحققًا أقوى

الاستيراد يتحقق من أن الملف JSON وأنه يحتوي مفتاحًا واحدًا على الأقل، لكنه لا يتحقق من أنواع البيانات وبنيتها وحدودها. كما أنه يبدأ الكتابة قبل التأكد من سلامة كل الأقسام، ولا ينشئ نسخة أمان تلقائية من الحالة الحالية قبل الاستبدال.

**التحسين المقترح:**

1. قراءة الملف والتحقق منه بالكامل أولًا.
2. التحقق من schema version وبنية كل قائمة.
3. إنشاء نسخة طوارئ تلقائية للحالة الحالية.
4. الاستيراد داخل عملية ذرية أو مع rollback.
5. إظهار ملخص لما سيتم استبداله قبل التنفيذ.

### أولوية متوسطة

#### 4. اختلاف واضح بين التوقيت المحلي وUTC

بعض الشاشات تستخدم `dayKeyLocal` للتعامل مع اليوم المحلي، بينما تستخدم مسارات أخرى `new Date().toISOString().slice(0,10)` لحساب اليوم أو الشهر. في مصر، بعد منتصف الليل المحلي وقبل منتصف الليل UTC، قد يظهر موعد اليوم كأنه موعد الأمس أو العكس، وقد تتأثر تقارير الشهر والتنبيهات.

**التحسين المقترح:** إنشاء دالة واحدة مثل `dateKeyLocal()` و`monthKeyLocal()` واستخدامها في كل التقارير، المواعيد، التنبيهات، أرقام الأوامر، والفلترة. يجب عدم استخدام `toISOString().slice(0,10)` لتمثيل تاريخ عمل محلي.

#### 5. تشغيل الترحيلات غير مركزي وقد يسبقها العرض

الترحيلات غير مستدعاة من نقطة دخول مركزية واحدة؛ يوجد استدعاء ضمن `app-quick-add.js` بينما توجد مستمعات `DOMContentLoaded` أخرى ترسم الواجهة. ولأن الترحيلات async، يمكن نظريًا أن يبدأ رسم شاشة قبل انتهاء الترحيل، خصوصًا عند فتح صفحات مختلفة مباشرة.

**التحسين المقترح:** إضافة bootstrap واحد لكل الصفحات، ينتظر `runMigrations()`، ثم يطلق حدثًا مثل `workshop:ready`، ولا يبدأ أي render قبل هذا الحدث.

#### 6. لا توجد اختبارات آلية للعمليات الحساسة

لم توجد اختبارات أو إعدادات lint/format في الملف المرفق. وجود 6,623 سطر JavaScript وعدد كبير من الصفحات يجعل الاعتماد على الفحص اليدوي وسجل التغييرات غير كافٍ، خصوصًا في دورة الحالة والمخزون والخزنة والمحافظ.

**التحسين المقترح:** إضافة اختبارات وحدات للوظائف الصرفية، وعلى الأقل:

- انتقالات حالات أمر الشغل.
- تعديل أمر ملغى أو إعادة فتحه.
- خصم وإرجاع المخزون.
- مزامنة العربون والتحصيل مع الخزنة والمحفظة.
- حذف أمر وتراجعه.
- النسخ الاحتياطي والاسترجاع.
- توحيد التواريخ المحلية.

ثم إضافة اختبار دخاني للصفحات الأساسية في متصفح آلي.

#### 7. الاعتماد الكبير على `onclick` وHTML المبني كسلاسل نصية

تم إصلاح مشكلات escaping سابقة، واستخدام `esc` و`escAttr` جيد في المواضع المفحوصة. لكن ما زال التطبيق يحتوي على عدد كبير من inline handlers وعمليات `innerHTML`. هذا يجعل الصيانة واكتشاف أخطاء السياقات أصعب، وقد يعيد إنتاج أخطاء escaping مستقبلية.

**التحسين المقترح:** الانتقال تدريجيًا إلى `addEventListener` و`data-*` attributes، واستخدام DOM APIs أو قوالب مركزية. كما يجب منع إدخال أي قيمة غير مهروبة داخل HTML أو JavaScript attribute.

### أولوية منخفضة / تحسينات جودة وأداء

#### 8. ملف قاعدة الأكواد كبير جدًا ويُحمّل كاملًا

`compressor-db.js` حجمه نحو 1.9 MB، رغم أن الواجهة تعرض 100 نتيجة فقط. هذا مقبول على جهاز حديث، لكنه يزيد زمن التحميل واستهلاك الذاكرة، خصوصًا على هاتف ضعيف أو شبكة بطيئة.

**التحسين المقترح:** فصل قاعدة الأكواد إلى JSON مضغوط أو ملفات حسب الماركة، أو بناء فهرس بحث مسبق، أو تحميلها lazy عند فتح صفحة الأكواد فقط. يمكن أيضًا استخدام Web Worker للبحث إذا زادت القاعدة مستقبلًا.

#### 9. عدم تطابق العدد المعلن لقاعدة الأكواد

الفحص الفعلي وجد **12,202 سجلًا**، بينما توجد عبارات واجهة/توثيق تشير إلى أكثر من **13,600 موديل**. هذا يسبب التباسًا للمستخدم.

**التحسين المقترح:** حساب العدد ديناميكيًا في كل النصوص، أو تحديث النص الثابت وفق العدد الفعلي بعد اعتماد مصدر البيانات.

#### 10. وجود تكرار وظيفي وضخامة في ملفات الواجهة

أكبر ملف هو `workshop-mini-simple-ui.js` بنحو 989 سطرًا، مع عدد من الملفات التي تعدّل دوال عرض عامة. يوجد نظام `defineOverride` لتسجيل الاستبدالات، وهو حل مراقبة جيد، لكنه مؤشر على أن البنية ما زالت تعتمد على global functions وترتيب تحميل الملفات.

**التحسين المقترح:** تقسيم الوحدات إلى modules واضحة، وتمرير البيانات إلى دوال العرض بدل الاعتماد على globals، وتحديد نقطة دخول لكل صفحة.

#### 11. تجربة فشل التخزين تحتاج تصميمًا أفضل

رسالة امتلاء `localStorage` مفيدة، لكن بعض العمليات قد تكون قد عدّلت جزءًا من الحالة قبل ظهور الرسالة. كما أن تخزين إعدادات النظام يتم في `settings()` أثناء القراءة، وهو سلوك قد يسبب كتابات متكررة وغير متوقعة.

**التحسين المقترح:** فصل القراءة عن الكتابة، إضافة فحص مساحة تخزين، إظهار حالة الحفظ للمستخدم، وجعل عمليات التعديل تعيد نتيجة موحدة قابلة للمعالجة.

## تقييم الأمان

لا توجد في الفحص مؤشرات على Backend مكشوف أو مفاتيح سرية داخل الملفات. كما أن النظام يستخدم escaping للنصوص المعروضة، وتمت معالجة عدة مواضع خطرة سابقًا. لكن هذا لا يساوي أمان تطبيق متعدد المستخدمين؛ فالبيانات المحلية قابلة للوصول لأي شخص يستخدم نفس الجهاز/المتصفح، ولا توجد صلاحيات أو سجل تدقيق أو تشفير فعلي.

إذا كانت البيانات تتضمن أرقام هواتف وعناوين وتسجيلات مكالمات، فالاستخدام الإنتاجي يحتاج على الأقل إلى حسابات مستخدمين، صلاحيات، نسخ احتياطي مشفر، HTTPS، وقاعدة بيانات مع سياسة احتفاظ للتسجيلات.

## خطة تحسين عملية مقترحة

### المرحلة الأولى: إصلاحات سريعة

1. توحيد كل حسابات اليوم والشهر على التاريخ المحلي.
2. تعديل `put()` والعمليات المستهلكة له لتتعامل مع الفشل صراحة.
3. تحويل إضافة القطعة، الحذف، والاستيراد إلى عمليات rollback موحدة.
4. إنشاء نسخة تلقائية قبل استيراد أي نسخة احتياطية.
5. تصحيح عدد سجلات أكواد الكباسات الظاهر للمستخدم.

### المرحلة الثانية: الاختبارات والبنية

1. إضافة اختبارات للمنطق الحسابي ودورة الحالات.
2. إضافة lint وformat وفحص روابط/ملفات تلقائي في CI.
3. إضافة bootstrap مركزي ينتظر الترحيلات.
4. تقليل inline handlers وglobal functions تدريجيًا.
5. إضافة فحص سلامة بيانات دوري يكتشف فروق المخزون والحركات والأوامر.

### المرحلة الثالثة: التحول الإنتاجي

1. نقل البيانات المترابطة إلى IndexedDB أو Backend حقيقي.
2. إضافة مستخدمين وصلاحيات وسجل تدقيق.
3. دعم مزامنة بين الأجهزة وحل تعارضات التعديل.
4. تشفير النسخ الاحتياطية وحماية التسجيلات.
5. إضافة اختبارات متصفح كاملة للرحلات اليومية.

## القرار النهائي

**النظام قابل للاستخدام الحالي كتطبيق محلي فردي، لكنه يحتاج تحسينًا قبل اعتباره نظامًا إنتاجيًا يعتمد عليه في أكثر من جهاز أو أكثر من مستخدم.** الأولوية ليست إعادة كتابة الواجهة بالكامل؛ الأفضل تنفيذ إصلاحات المعاملات والتواريخ والاسترجاع أولًا، ثم إضافة الاختبارات، وبعدها اتخاذ قرار نقل التخزين إلى Backend حسب طريقة التشغيل المطلوبة.

لم يتم تعديل ملفات المشروع في هذه المراجعة؛ التقرير مبني على الفحص الساكن وتشغيل فحوصات الصياغة والروابط وبنية قاعدة الأكواد.
