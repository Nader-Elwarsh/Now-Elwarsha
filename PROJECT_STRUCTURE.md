# هيكل مشروع الورشة الفنية

## طبيعة التطبيق

المشروع تطبيق PWA محلي لمستخدم واحد. البيانات التشغيلية محفوظة في `localStorage`، بينما الصور والتسجيلات في `IndexedDB`. لا توجد خدمة Backend أو مزامنة بين أجهزة في الإصدار الحالي.

## طبقات المشروع

| الطبقة | الملفات الرئيسية | المسؤولية |
|---|---|---|
| التخزين والمشترك | `shared-data.js`, `image-store.js`, `share-store.js` | مفاتيح البيانات، القراءة والكتابة، rollback، الصور والمشاركة |
| الترحيلات | `migrations.js` | نقل شكل البيانات القديمة إلى المخطط الحالي |
| المجال التشغيلي | `app-requests.js`, `app-parts.js`, `app-restock.js`, `wallets.js`, `treasury.js` | أوامر الشغل، المخزن، المحافظ، والخزنة |
| الواجهة والتقارير | `app-shared.js`, `app-dashboard-reports.js`, `workshop-mini-simple-ui.js` | العرض المشترك، لوحة التحكم، والتقارير |
| الإدارة | `app-settings.js`, `app-settings-lists.js`, `app-data-management.js`, `app-delete-tools.js` | الإعدادات، النسخ، الاسترجاع، والحذف |
| الأكواد المرجعية | `app-compressor-codes.js`, `compressor-index.js`, `brand-000.js` … `brand-057.js` | البحث في أكواد الكباسات، البدائل من شركات تانية، الإضافات اليدوية ومشاركتها |
| الاختبارات | `*-tests.js`, `*-check.js`, `*-smoke.js`, `browser-smoke.sh` | فحص الصياغة، المعاملات، الترحيلات، النسخ، ومسارات الأكواد |

> ملحوظة: كل ملفات المشروع (بما فيها ملفات الماركات وملفات الاختبار) موجودة في مستوى واحد بلا مجلدات فرعية، تسهيلًا لرفع المشروع عبر أدوات لا تدعم رفع مجلدات متداخلة دفعة واحدة. `compressor-index.js` و`service-worker.js` و`package.json` يشيرون لكل الملفات بأسمائها المباشرة من غير أي بادئة مسار.

> ملحوظة: `app-compressor-codes.js` محمّل على كل صفحات النظام (مش صفحة الأكواد بس) عشان يوفّر `window.CompressorRef` لِـ `global-search.js`، اللي بيضيف قسم "أكواد الكباسات" لنتائج البحث الشامل من أي صفحة. القاعدة نفسها بتتحمّل كسول لما المستخدم يفتح صندوق البحث الشامل أو صفحة الأكواد فعليًا، مش قبل كده.

## قواعد التعديل

ينبغي أن تمر أي عملية تلمس أكثر من مفتاح تخزين عبر `withRollback` أو `commitStorage`. يجب فحص القيمة المرجعة من كل عملية حفظ، وعدم عرض رسالة نجاح قبل التأكد من نجاح التخزين.

تستخدم تواريخ العمل الدالة المحلية الموحدة بدل استخراج اليوم من `toISOString()`. وتستخدم أكواد الكباسات مفتاح مقارنة موحدًا للماركة والموديل لمنع التكرار.

## الأداء

قاعدة الأكواد تُحمّل عند فتح صفحة الأكواد فقط. يُحمّل التطبيق `compressor-index.js` أولًا، ثم ملف الماركة المطلوبة عند استخدام فلتر ماركة، أو كل ملفات الماركات عند تنفيذ بحث حر. بعد ذلك يبني سجلات بحث مفهرسة مرة واحدة ويعيد استخدام نتائج البحث مؤقتًا. لا تُحمّل قاعدة الأكواد من الصفحات الأخرى.

## التحقق قبل الإصدار

```text
npm run check
```

يتضمن الأمر فحص صياغة ملفات JavaScript واختبارات التاريخ والنسخ والترحيلات والتراجع ومسارات الأكواد. كما يجب فحص الروابط المحلية وتحديث نسخة Service Worker عند تعديل ملفات الواجهة.

## حدود معروفة

ما زالت بعض صفحات التطبيق القديمة تستخدم `onclick` و`innerHTML`، ويجري تحويلها تدريجيًا حسب كل صفحة لتقليل مخاطر التغيير الواسع. نقل البيانات إلى قاعدة IndexedDB كاملة أو Backend ومزامنة متعددة الأجهزة يتطلب مرحلة تصميم مستقلة.
